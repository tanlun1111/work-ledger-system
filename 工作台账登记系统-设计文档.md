# 警情案件工作台账登记系统 - 设计文档

## 一、项目概述

### 1.1 项目名称

**警情案件工作台账登记系统**

### 1.2 项目目标

构建一个基于 Web 的警情案件工作台账管理平台，实现警情案件从接报、处置、研判到闭环的全流程登记与跟踪，支持数据持久化存储、图片附件上传和多维度检索。

### 1.3 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端框架 | Python Flask | 轻量级 Web 框架，快速开发 |
| 数据库 | SQLite | 嵌入式关系型数据库，零配置，文件级存储 |
| ORM | SQLAlchemy | Python ORM 框架，支持 SQLite |
| 前端模板 | Jinja2 | Flask 内置模板引擎 |
| 前端样式 | Bootstrap 5 | 响应式 UI 框架 |
| 前端交互 | 原生 JavaScript + Fetch API | 无需额外框架依赖 |
| 图片存储 | 本地文件系统 (uploads/) | 图片附件保存到服务器本地目录 |

### 1.4 运行环境

- Python 3.8+
- 操作系统：Windows / Linux / macOS
- 浏览器：Chrome / Edge / Firefox 最新版

---

## 二、数据库设计

### 2.1 数据表：work_ledger（工作台账主表）

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 说明 |
|------|--------|---------|------|------|------|
| 1 | id | 主键ID | INTEGER | PK, AUTOINCREMENT | 自增主键 |
| 2 | serial_no | 序号 | VARCHAR(20) | NOT NULL, UNIQUE | 可自定义或自动生成（如 JQ-20260715-001） |
| 3 | duty_officer | 直守人员 | VARCHAR(50) | NOT NULL | 值班执勤人员姓名 |
| 4 | personnel | 涉及人员 | TEXT | | 案件/警情涉及人员 |
| 5 | incident_time | 发生时间 | DATETIME | NOT NULL | 警情/案件发生时间 |
| 6 | info_source | 信息来源 | VARCHAR(100) | NOT NULL | 警情信息来源（如 110接报、群众报案、上级交办、其他部门移交等） |
| 7 | jurisdiction | 辖区 | VARCHAR(100) | NOT NULL | 所属辖区 |
| 8 | case_category | 警情案件类别 | VARCHAR(50) | NOT NULL | 案件类型分类（如 刑事、治安、交通、求助等） |
| 9 | case_basic_info | 案件或警情基本情况 | TEXT | NOT NULL | 案件基本描述 |
| 10 | on_site_personnel | 现场处置人员 | VARCHAR(200) | | 到场处置的人员名单 |
| 11 | on_site_handling | 现场开展处置情况 | TEXT | | 现场处置过程和措施 |
| 12 | analysis_situation | 研判情况 | TEXT | | 研判分析结果 |
| 13 | closure_status | 闭环情况 | TEXT | | 闭环处理情况说明 |
| 14 | follow_up | 后续移交跟进 | TEXT | | 后续移交处理记录 |
| 15 | handover | 人员交接 | TEXT | | 交接班人员记录 |
| 16 | is_closed | 是否办结 | BOOLEAN | NOT NULL, DEFAULT 0 | 0=未办结, 1=已办结 |
| 17 | is_escalated | 是否提级 | BOOLEAN | NOT NULL, DEFAULT 0 | 0=未提级, 1=已提级（需上级处理） |
| 18 | created_at | 创建时间 | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 记录创建时间 |
| 19 | updated_at | 更新时间 | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 记录最后更新时间 |

> **注意**：研判图片附件已从 `work_ledger` 表中移除，改为独立关联表 `ledger_images`（见 2.3）。

### 2.2 索引设计

| 索引名 | 字段 | 说明 |
|--------|------|------|
| idx_serial_no | serial_no | 序号唯一索引 |
| idx_incident_time | incident_time | 按时间检索加速 |
| idx_jurisdiction | jurisdiction | 按辖区检索加速 |
| idx_case_category | case_category | 按类别检索加速 |
| idx_created_at | created_at | 按创建时间排序 |
| idx_closed_time | (is_closed, incident_time) | 按办结状态+时间组合检索 |

> **注意**：未对 `is_closed` 和 `is_escalated` 建单列索引，因二值字段选择性仅 50%，B-Tree 索引无法被优化器有效利用。改用组合索引 `idx_closed_time` 覆盖"已办结案件按时间倒序"等高频查询。

### 2.3 数据表：ledger_images（图片附件表）

研判图片改为独立关联表，支持 UUID 安全重命名、原始文件名记录、MIME 校验和独立查询：

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 说明 |
|------|--------|---------|------|------|------|
| 1 | id | 主键ID | INTEGER | PK, AUTOINCREMENT | — |
| 2 | ledger_id | 台账ID | INTEGER | NOT NULL, FK→work_ledger.id, INDEX | 关联台账记录 |
| 3 | uuid_filename | 存储文件名 | VARCHAR(255) | NOT NULL | UUID + 原始扩展名 |
| 4 | original_name | 原始文件名 | VARCHAR(255) | NOT NULL | 用户上传时的文件名 |
| 5 | file_size | 文件大小 | INTEGER | NOT NULL | 字节数 |
| 6 | mime_type | MIME类型 | VARCHAR(50) | NOT NULL | 如 `image/jpeg` |
| 7 | sort_order | 排序 | INTEGER | DEFAULT 0 | 图片展示顺序 |
| 8 | created_at | 上传时间 | DATETIME | DEFAULT CURRENT_TIMESTAMP | — |

---

## 三、系统架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                        浏览器                            │
│              (Bootstrap 5 + 原生 JS)                     │
└─────────────────────┬───────────────────────────────────┘
                      │  HTTP Request / Response
                      ▼
┌─────────────────────────────────────────────────────────┐
│                    Flask Web 应用                        │
│  ┌───────────┐  ┌───────────┐  ┌───────────────────┐   │
│  │  路由层    │  │  业务逻辑层 │  │   数据访问层       │   │
│  │ (routes)  │─▶│ (services)│─▶│ (SQLAlchemy ORM)  │   │
│  └───────────┘  └───────────┘  └─────────┬─────────┘   │
│                                           │              │
│                                ┌──────────▼──────────┐  │
│                                │     SQLite 数据库    │  │
│                                └─────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 3.2 分层说明

| 层级 | 目录/文件 | 职责 |
|------|----------|------|
| 路由层 (Routes) | `app/routes/` | 处理 HTTP 请求，参数校验，调用业务层 |
| 业务逻辑层 (Services) | `app/services/` | 封装核心业务逻辑，数据处理 |
| 数据访问层 (Models) | `app/models/` | 数据库模型定义，ORM 映射 |
| 工具层 (Utils) | `app/utils/` | 通用工具函数（文件上传、日期格式化等） |
| 模板层 (Templates) | `app/templates/` | Jinja2 前端模板页面 |
| 静态资源 (Static) | `app/static/` | CSS、JS、图片等静态文件 |

### 3.3 数据流转

```
用户操作 → 前端页面 → Fetch API 异步请求 → Flask 路由
    → 参数校验 → 业务逻辑处理 → ORM 数据库操作 → 返回 JSON 响应
    → 前端渲染 / 页面跳转
```

---

## 四、API 接口设计

### 4.1 通用约定

- **Base URL**: `http://localhost:5000`
- **请求格式**: `application/json`（除文件上传使用 `multipart/form-data`）
- **响应格式**: 统一 JSON 结构

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

| code | 含义 |
|------|------|
| 200 | 操作成功 |
| 400 | 参数校验失败 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 4.2 台账登记 API

#### 4.2.1 新增台账记录

```
POST /api/ledger
Content-Type: application/json
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| serial_no | string | 否 | 序号，不填则自动生成 |
| duty_officer | string | 是 | 直守人员 |
| personnel | string | 否 | 涉及人员 |
| incident_time | string | 是 | 发生时间 (YYYY-MM-DD HH:mm) |
| info_source | string | 是 | 信息来源 |
| jurisdiction | string | 是 | 辖区 |
| case_category | string | 是 | 警情案件类别 |
| case_basic_info | string | 是 | 案件基本描述 |
| on_site_personnel | string | 否 | 现场处置人员 |
| on_site_handling | string | 否 | 现场处置情况 |
| analysis_situation | string | 否 | 研判情况 |
| closure_status | string | 否 | 闭环情况 |
| follow_up | string | 否 | 后续移交跟进 |
| handover | string | 否 | 人员交接 |
| is_closed | int | 否 | 是否办结 (0/1) |
| is_escalated | int | 否 | 是否提级 (0/1) |

> **注意**：图片上传已拆分为独立接口 `POST /api/ledger/<id>/images`，新建台账时先提交文本数据获取 ID，再上传图片。

**响应示例**:

```json
{
  "code": 200,
  "message": "台账登记成功",
  "data": {
    "id": 1,
    "serial_no": "JQ-20260715-001"
  }
}
```

#### 4.2.2 编辑台账记录

```
PUT /api/ledger/<id>
Content-Type: application/json
```

参数同新增接口，所有参数可选（只提交需要修改的字段）。

#### 4.2.3 删除台账记录

```
DELETE /api/ledger/<id>
```

#### 4.2.4 查询台账详情

```
GET /api/ledger/<id>
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "serial_no": "JQ-20260715-001",
    "duty_officer": "张三",
    "personnel": "李四、王五",
    "incident_time": "2026-07-15 14:30",
    "info_source": "110接报",
    "jurisdiction": "XX派出所辖区",
    "case_category": "治安",
    "case_basic_info": "...",
    "on_site_personnel": "民警甲、民警乙",
    "on_site_handling": "...",
    "analysis_situation": "...",
    "closure_status": "...",
    "follow_up": "...",
    "handover": "...",
    "is_closed": 0,
    "is_escalated": 0,
    "created_at": "2026-07-15 15:00:00",
    "updated_at": "2026-07-15 15:00:00",
    "images": [
      {
        "id": 1,
        "uuid_filename": "a1b2c3d4.jpg",
        "original_name": "现场照片.jpg",
        "file_size": 204800,
        "url": "/api/ledger/1/image/a1b2c3d4.jpg"
      }
    ]
  }
}
```

#### 4.2.5 台账列表查询（分页+筛选）

```
GET /api/ledger?page=1&per_page=20&keyword=&jurisdiction=&case_category=&is_closed=&is_escalated=&date_from=&date_to=
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码，默认 1 |
| per_page | int | 否 | 每页条数，默认 20 |
| keyword | string | 否 | 关键词搜索（匹配序号、人员、基本情况） |
| jurisdiction | string | 否 | 按辖区筛选 |
| case_category | string | 否 | 按案件类别筛选 |
| is_closed | int | 否 | 按办结状态筛选 |
| is_escalated | int | 否 | 按提级状态筛选 |
| date_from | string | 否 | 发生时间起始 (YYYY-MM-DD) |
| date_to | string | 否 | 发生时间截止 (YYYY-MM-DD) |

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "items": [...],
    "total": 156,
    "page": 1,
    "per_page": 20,
    "total_pages": 8
  }
}
```

#### 4.2.6 通用枚举值查询

```
GET /api/enums
```

**响应**: 返回信息来源、案件类别等枚举选项列表，供前端下拉框使用。

### 4.3 图片相关 API

#### 4.3.1 上传图片

```
POST /api/ledger/<id>/images
Content-Type: multipart/form-data
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| files | file[] | 是 | 图片文件，允许多文件一次上传 |

**安全约束**：允许类型为 `image/jpeg, image/png, image/gif, image/webp`，单文件最大 10MB。文件以 UUID 重命名存储于 `uploads/` 目录。

**新增台账时先创建台账（`POST /api/ledger`）获取 ID，再上传图片。**

#### 4.3.2 预览图片

```
GET /api/ledger/<id>/images/<uuid_filename>
```

通过 API 层返回图片文件流（非直接访问静态文件），未来可在此处加入权限判断。

#### 4.3.3 删除图片

```
DELETE /api/ledger/<id>/images/<image_id>
```

从数据库和文件系统同时移除指定图片。

---

## 五、前端页面设计

### 5.1 页面路由规划

| 路由 | 页面名称 | 说明 |
|------|---------|------|
| `/` | 台账列表页 | 默认首页，展示台账列表，支持分页和检索 |
| `/ledger/add` | 新增台账页 | 台账登记表单 |
| `/ledger/<id>/edit` | 编辑台账页 | 编辑已有台账记录 |
| `/ledger/<id>/view` | 台账详情页 | 查看台账完整信息 |

### 5.2 台账列表页

**布局**: 顶部搜索栏 + 筛选栏 + 中间数据表格 + 底部分页

**搜索栏**:
- 关键词输入框（搜索序号、涉及人员、基本情况）
- 辖区下拉筛选
- 案件类别下拉筛选
- 办结状态筛选（未办结 / 已办结 / 全部）
- 提级状态筛选
- 时间范围筛选（发生时间起止）
- 搜索按钮 + 重置按钮

**数据表格列**:

| 列名 | 宽度 | 说明 |
|------|------|------|
| 序号 | 120px | serial_no |
| 直守人员 | 80px | duty_officer |
| 发生时间 | 130px | incident_time |
| 辖区 | 100px | jurisdiction |
| 案件类别 | 90px | case_category |
| 信息来源 | 90px | info_source |
| 基本情况 | 200px | case_basic_info（超长省略） |
| 办结状态 | 80px | 标签：已办结(绿) / 未办结(橙) |
| 提级状态 | 80px | 标签：已提级(红) / 未提级(灰) |
| 操作 | 150px | 查看 / 编辑 / 删除 按钮 |

### 5.3 新增/编辑台账页

**表单分区设计**（采用 Bootstrap Accordion 折叠面板）：

**面板一：基本信息**（默认展开）
- 序号（自动生成，可手动修改）
- 直守人员（必填）
- 涉及人员
- 发生时间（必填，datetime-local 选择器）
- 信息来源（必填，下拉选择）
- 辖区（必填，输入框）
- 案件类别（必填，下拉选择）
- 案件基本情况（必填，textarea，4行）

**面板二：现场处置**
- 现场处置人员
- 现场开展处置情况（textarea，4行）

**面板三：研判分析**
- 研判情况（textarea，4行）
- 研判图片上传（多文件拖拽/选择上传，支持预览和删除）

**面板四：闭环与交接**
- 闭环情况（textarea，3行）
- 后续移交跟进（textarea，3行）
- 人员交接（textarea，3行）
- 是否办结（开关/复选框）
- 是否提级（开关/复选框）

**底部按钮**: 提交保存 + 取消返回

### 5.4 台账详情页

采用卡片布局展示台账完整信息，分为四个信息区块：

1. **基本信息卡片** — 展示序号、人员、时间、来源、辖区、类别、基本情况
2. **处置信息卡片** — 展示现场处置人员和情况
3. **研判信息卡片** — 展示研判情况 + 图片附件缩略图（点击可放大查看）
4. **闭环信息卡片** — 展示闭环状态、后续跟进、交接记录、办结/提级标签

底部操作栏：返回列表 + 编辑按钮

### 5.5 交互规范

- 表单提交使用异步 Fetch，附带 Loading 状态和 Toast 提示
- 删除操作弹出确认对话框（Bootstrap Modal）
- 图片上传支持预览缩略图，支持删除已上传图片
- 列表页支持条件记忆（筛选条件保存到 URL query string）
- 新建/编辑页面离开时检测表单是否有未保存修改，给出提示

---

## 六、业务流程图

### 6.1 核心业务流程

```
┌──────────┐
│  接报登记  │  录入基本信息（人员、时间、来源、辖区、类别）
└────┬─────┘
     │
     ▼
┌──────────┐
│  现场处置  │  记录现场处置人员和处置情况
└────┬─────┘
     │
     ▼
┌──────────┐
│  研判分析  │  撰写研判情况、上传图片附件
└────┬─────┘
     │
     ├── 需要提级 ──▶ 标记"已提级" → 移交上级处理
     │
     ▼
┌──────────┐
│  闭环处理  │  记录闭环情况、后续移交、人员交接
└────┬─────┘
     │
     ▼
┌──────────┐
│  标记办结  │  标记 is_closed = 1
└──────────┘
```

### 6.2 系统交互流程

```
用户 → 登录系统 → 台账列表页
                     │
          ┌──────────┼──────────┐
          ▼          ▼          ▼
       新增台账    查看详情    搜索筛选
          │          │          │
          ▼          ▼          ▼
       填写表单    编辑/删除   分页浏览
          │
          ▼
       提交保存 → 表单校验 → 文件上传 → 数据库写入 → 刷新列表
```

---

## 七、项目目录结构

```
work-ledger-system/
├── run.py                     # 应用启动入口
├── config.py                  # 配置文件（数据库路径、上传目录等）
├── requirements.txt           # Python 依赖清单
│
├── app/                       # 应用主目录
│   ├── __init__.py            # Flask 应用工厂
│   │
│   ├── models/                # 数据模型层
│   │   ├── __init__.py
│   │   └── ledger.py          # WorkLedger ORM 模型定义
│   │
│   ├── routes/                # 路由层
│   │   ├── __init__.py
│   │   ├── main.py            # 页面路由（首页、详情页等）
│   │   └── api.py             # API 路由（CRUD 接口）
│   │
│   ├── services/              # 业务逻辑层
│   │   ├── __init__.py
│   │   └── ledger_service.py  # 台账业务逻辑
│   │
│   ├── utils/                 # 工具函数
│   │   ├── __init__.py
│   │   ├── helpers.py         # 通用辅助函数（序号生成、日期处理）
│   │   └── upload.py          # 图片上传/删除工具
│   │
│   ├── templates/             # Jinja2 模板
│   │   ├── base.html          # 基础布局模板（导航栏、页脚、公共CSS/JS）
│   │   ├── index.html         # 台账列表页
│   │   ├── ledger_form.html   # 新增/编辑台账页（共用表单模板）
│   │   └── ledger_detail.html # 台账详情页
│   │
│   └── static/                # 静态资源
│       ├── css/
│       │   └── style.css      # 自定义样式
│       └── js/
│           └── main.js        # 自定义 JavaScript 逻辑
│
├── uploads/                   # 图片上传存储目录（运行时自动创建）
│   └── .gitkeep
│
└── instance/                  # Flask 实例文件夹（SQLite 数据库文件存放处）
    └── (work_ledger.db)       # 运行时自动生成
```

---

## 八、部署说明

### 8.1 环境准备

```bash
# 1. 确保安装 Python 3.8+
python --version

# 2. 创建虚拟环境（推荐）
python -m venv venv

# 3. 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# 4. 安装依赖
pip install -r requirements.txt
```

### 8.2 requirements.txt

```
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1
```

### 8.3 启动应用

```bash
# 开发模式启动（默认端口 5000，开启 Debug）
python run.py
```

启动后访问 `http://localhost:5000` 即可使用系统。

### 8.4 生产部署建议

- 使用 `gunicorn`（Linux）或 `waitress`（Windows）作为 WSGI 服务器
- 设置 `FLASK_ENV=production` 关闭 Debug 模式
- 定期备份 `instance/work_ledger.db` 数据库文件和 `uploads/` 目录
- 可选使用 Nginx 做反向代理和静态文件服务

---

## 九、待扩展功能（后续版本）

- [ ] 用户认证与权限管理（登录/角色/操作权限）
- [ ] 数据导出功能（Excel / PDF）
- [ ] 操作日志审计
- [ ] 数据统计看板（Dashboard，图表展示）
- [ ] 批量导入功能
