# 警情案件工作台账登记系统 - 后端设计文档

## 一、项目概述

### 1.1 项目名称

警情案件工作台账登记系统（后端）

### 1.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端框架 | Python Flask 3.x | 轻量级 Web 框架 |
| 数据库 | MySQL 5.7+ | 通过 PyMySQL 驱动连接，默认 `localhost:3306` |
| ORM | SQLAlchemy（Flask-SQLAlchemy） | Python ORM 框架 |
| 迁移工具 | Flask-Migrate（Alembic） | 数据库版本管理与增量迁移 |
| 序列化 | Marshmallow | 请求参数校验与响应序列化 |
| 认证 | Flask 内置 Session | 服务端 Cookie 会话，`@login_required` 装饰器拦截 |
| 密码哈希 | Werkzeug | `generate_password_hash` / `check_password_hash`（pbkdf2:sha256） |
| 表单保护 | Flask-WTF CSRF | 跨站请求伪造防护 |
| 前端模板 | Jinja2 | Flask 内置模板引擎 |
| 图片存储 | 本地文件系统（`uploads/`） | UUID 重命名，API 层鉴权访问 |

### 1.3 运行环境

- Python 3.8+
- MySQL 5.7+（`localhost:3306`，数据库名 `work_ledger_dev` / `work_ledger`）
- 操作系统： Windows / Linux / macOS
- 数据库字符集： `utf8mb4`（启动时自动建库）

---

## 二、数据库设计

### 2.1 MySQL 连接与类型映射

连接串格式：`mysql+pymysql://root:root@localhost:3306/work_ledger_dev?charset=utf8mb4`

| ORM 声明类型 | MySQL 实际类型 | 说明 |
|---------|---------|------|
| `VARCHAR(n)` | VARCHAR(n) | MySQL 原生约束长度 |
| `TEXT` | TEXT | 最大 65535 字节 |
| `INTEGER` | INT | 整数类型 |
| `DATETIME` | DATETIME | 无时区，统一使用本地时间 |
| `db.Integer`（布尔） | TINYINT(1) | 0 或 1，应用层保证 |

### 2.2 数据表：work_ledger（工作台账主表）

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 默认值 | 校验规则 |
|------|--------|---------|------|------|--------|---------|
| 1 | id | 主键ID | INTEGER | PK, AUTO_INCREMENT | — | — |
| 2 | serial_no | 序号 | VARCHAR(30) | NOT NULL, UNIQUE | 自动生成 | 格式 `JQ-YYYYMMDD-NNNN` 或自定义 ≤ 30 字符 |
| 3 | duty_officer | 直守人员 | VARCHAR(50) | NOT NULL | — | 1~50 字符 |
| 4 | personnel | 涉及人员 | TEXT | — | NULL | ≤ 2000 字符 |
| 5 | incident_time | 发生时间 | DATETIME | NOT NULL, INDEX | — | 格式 `YYYY-MM-DD HH:MM` |
| 6 | info_source | 信息来源 | VARCHAR(100) | NOT NULL | — | 枚举值校验 |
| 7 | jurisdiction | 辖区 | VARCHAR(100) | NOT NULL | — | 1~100 字符 |
| 8 | case_category | 警情案件类别 | VARCHAR(50) | NOT NULL | — | 枚举值校验 |
| 9 | case_basic_info | 案件基本情况 | TEXT | NOT NULL | — | 1~5000 字符 |
| 10 | on_site_personnel | 现场处置人员 | VARCHAR(200) | — | NULL | ≤ 200 字符 |
| 11 | on_site_handling | 现场处置情况 | TEXT | — | NULL | ≤ 5000 字符 |
| 12 | analysis_situation | 研判情况 | TEXT | — | NULL | ≤ 5000 字符 |
| 13 | closure_status | 闭环情况 | TEXT | — | NULL | ≤ 3000 字符 |
| 14 | follow_up | 后续移交跟进 | TEXT | — | NULL | ≤ 3000 字符 |
| 15 | handover | 人员交接 | TEXT | — | NULL | ≤ 2000 字符 |
| 16 | is_closed | 是否办结 | INTEGER | NOT NULL | 0 | 0 或 1 |
| 17 | is_escalated | 是否提级 | INTEGER | NOT NULL | 0 | 0 或 1 |
| 18 | is_deleted | 是否删除 | INTEGER | NOT NULL | 0 | 0 或 1，软删除标记 |
| 19 | created_at | 创建时间 | DATETIME | NOT NULL | CURRENT_TIMESTAMP | — |
| 20 | updated_at | 更新时间 | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | — |

### 2.3 数据表：ledger_images（图片附件表）

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 说明 |
|------|--------|---------|------|------|------|
| 1 | id | 主键ID | INTEGER | PK, AUTO_INCREMENT | — |
| 2 | ledger_id | 台账ID | INTEGER | NOT NULL, FK→work_ledger.id, INDEX | 关联台账记录 |
| 3 | uuid_filename | 存储文件名 | VARCHAR(255) | NOT NULL | UUID + 原始扩展名，如 `a1b2c3d4.jpg` |
| 4 | original_name | 原始文件名 | VARCHAR(255) | NOT NULL | 用户上传时的文件名 |
| 5 | file_size | 文件大小 | INTEGER | NOT NULL | 字节数 |
| 6 | mime_type | MIME类型 | VARCHAR(50) | NOT NULL | 如 `image/jpeg` |
| 7 | sort_order | 排序 | INTEGER | DEFAULT 0 | 图片展示顺序 |
| 8 | created_at | 上传时间 | DATETIME | DEFAULT CURRENT_TIMESTAMP | — |

### 2.4 数据表：enum_config（枚举配置表）

使用 `code` + `label` 分离设计，`code` 为内部编码（不可变），`label` 为显示文本（可修改）。

| 序号 | 字段名 | 类型 | 约束 | 说明 |
|------|--------|------|------|------|
| 1 | id | INTEGER | PK, AUTO_INCREMENT | — |
| 2 | enum_type | VARCHAR(50) | NOT NULL, INDEX | 枚举类型标识（`info_source`、`case_category`） |
| 3 | enum_code | VARCHAR(50) | NOT NULL | 内部编码（如 `110_report`），不可变，API 传参用 |
| 4 | enum_label | VARCHAR(100) | NOT NULL | 显示文本（如 `110接报`），可修改 |
| 5 | sort_order | INTEGER | DEFAULT 0 | 排序权重 |
| 6 | is_active | INTEGER | DEFAULT 1 | 是否启用 |

默认预置数据：

```
info_source:  110_report → 110接报,  citizen_report → 群众报案,  superior_assign → 上级交办,  dept_transfer → 其他部门移交,  proactive → 主动发现,  other → 其他
case_category: criminal → 刑事,  public_security → 治安,  traffic → 交通,  assistance → 求助,  dispute → 纠纷,  other → 其他
```

### 2.5 数据表：sys_user（系统用户表）

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 默认值 | 说明 |
|------|--------|---------|------|------|--------|------|
| 1 | id | 主键ID | INTEGER | PK, AUTO_INCREMENT | — | — |
| 2 | username | 用户名 | VARCHAR(50) | NOT NULL, UNIQUE | — | 登录用户名 |
| 3 | password_hash | 密码哈希 | VARCHAR(255) | NOT NULL | — | Werkzeug pbkdf2:sha256 哈希 |
| 4 | display_name | 显示名称 | VARCHAR(50) | NOT NULL | — | 页面展示的姓名 |
| 5 | role | 角色 | VARCHAR(20) | NOT NULL | 'user' | `admin` 或 `user` |
| 6 | is_active | 是否启用 | INTEGER | NOT NULL | 1 | 0=禁用, 1=启用 |
| 7 | last_login_at | 最后登录时间 | DATETIME | — | NULL | 记录上次登录时间 |
| 8 | created_at | 创建时间 | DATETIME | NOT NULL | CURRENT_TIMESTAMP | — |
| 9 | updated_at | 更新时间 | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | — |

角色说明：

| 角色 | 值 | 权限 |
|------|-----|------|
| 管理员 | `admin` | 查看/新增/编辑/删除所有台账、管理枚举配置、管理用户 |
| 普通用户 | `user` | 查看/新增/编辑台账、无法删除 |

默认管理员账号（首次启动自动创建，启动后应立即修改密码）：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| `admin` | `admin123` | admin |

### 2.6 索引设计

| 索引名 | 所属表 | 字段 | 类型 | 说明 |
|--------|--------|------|------|------|
| idx_serial_no | work_ledger | serial_no | UNIQUE | 序号唯一 |
| idx_incident_time | work_ledger | incident_time | 普通 | 发生时间排序/筛选 |
| idx_jurisdiction | work_ledger | jurisdiction | 普通 | 辖区筛选 |
| idx_case_category | work_ledger | case_category | 普通 | 类别筛选 |
| idx_created_at | work_ledger | created_at | 普通 | 创建时间排序 |
| idx_is_deleted | work_ledger | is_deleted | 普通 | 软删除过滤 |
| idx_closed_time | work_ledger | (is_closed, incident_time) | **组合** | 按办结+时间检索 |
| idx_ledger_id | ledger_images | ledger_id | 普通 | 图片表关联查询 |
| idx_username | sys_user | username | UNIQUE | 用户名唯一 |

---

## 三、系统架构设计

### 3.1 分层架构

```
┌──────────────────────────────────────────────────────┐
│                    路由层 (Routes)                     │
│   auth.py (认证路由)  │  main.py (页面路由)            │
│   api.py (RESTful API 路由)                           │
│   参数校验 (Marshmallow Schema)                       │
└──────────────────────┬───────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────┐
│                  业务逻辑层 (Services)                 │
│   ledger_service.py  │  image_service.py              │
│   enum_service.py    │  serial_service.py             │
│   事务管理  │  数据校验  │  业务编排                   │
└──────────────────────┬───────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────┐
│                 数据访问层 (Models)                    │
│   ledger.py  │  ledger_image.py  │  enum_config.py    │
│   user.py                               │            │
│   SQLAlchemy ORM  │  查询构建  │  关系映射            │
└──────────────────────┬───────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────┐
│            MySQL 数据库 (localhost:3306)               │
└──────────────────────────────────────────────────────┘

横切工具层 (Utils):
├── helpers.py      # 通用函数（日期格式化、字符串处理）
├── upload.py       # 文件上传、校验、存储
├── response.py     # 统一响应格式构造
└── auth.py         # login_required / admin_required 装饰器

横切错误层 (Errors):
├── exceptions.py   # 自定义异常类
└── __init__.py     # 全局错误处理器注册
```

### 3.2 各层职责

| 层级 | 目录/文件 | 职责 |
|------|----------|------|
| 路由层 | `app/routes/` | HTTP 请求接收、Marshmallow 参数校验、认证拦截、调用 Service、返回 JSON/HTML |
| 业务逻辑层 | `app/services/` | 核心业务逻辑、数据库事务控制、图片+数据原子操作编排 |
| 数据访问层 | `app/models/` | ORM 模型定义、通用查询封装、关联关系、初始数据播种 |
| 工具层 | `app/utils/` | 纯函数工具（无业务依赖）、文件操作、统一响应封装、认证装饰器 |
| 错误处理 | `app/errors/` | 自定义异常类 + 全局错误处理器 |
| 配置 | `config.py` | 多环境配置类（Development / Production），含 MySQL 连接串与 Session 配置 |

### 3.3 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

| code | 含义 |
|------|------|
| 200 | 操作成功 |
| 400 | 参数校验失败 |
| 401 | 未认证（未登录或 session 过期） |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突（如序号重复） |
| 500 | 服务器内部错误 |

### 3.4 全局异常处理

- `400 Bad Request` — Marshmallow ValidationError / 自定义 ValidationException
- `401 Unauthorized` — 未登录时 API 返回 JSON，页面请求重定向 `/login`
- `403 Forbidden` — `@admin_required` 拦截非管理员
- `404 Not Found` — 自定义 NotFoundException
- `409 Conflict` — 自定义 ConflictException
- `500 Internal Server Error` — 未捕获的 Exception，日志记录完整堆栈

### 3.5 认证体系

**认证方式**：Flask 内置 Session（服务端 Cookie），`@login_required` / `@admin_required` 装饰器。

**认证中间件（`app/utils/auth.py`）**：

- `login_required` — 从 session 读取 `user_id`，查询 User，验证 `is_active`，注入 `g.current_user`，将 user 对象作为第一个参数传递给路由函数
- `admin_required` — 独立验证登录态 + 校验 `user.role == "admin"`，不依赖 `@login_required` 注入

**模板全局注入**：通过 `@app.context_processor` 注册 `inject_current_user()`，优先从 `g.current_user` 读取（避免重复查库），使所有模板可直接访问 `{{ current_user }}`。

**Session 配置**：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `SESSION_COOKIE_HTTPONLY` | True | 防 XSS 窃取 Cookie |
| `SESSION_COOKIE_SAMESITE` | Lax | 防 CSRF |
| `PERMANENT_SESSION_LIFETIME` | timedelta(hours=24) | 24 小时无操作过期 |

**前端 401 拦截**：`main.js` 的 `api.request()` 中检测 `json.code === 401`，自动 `window.location.href = '/login?expired=1'`。

---

## 四、API 接口设计

### 4.1 通用约定

- Base URL： `http://localhost:5000`
- 文本数据接口： `Content-Type: application/json`
- 文件上传接口： `Content-Type: multipart/form-data`
- CSRF 保护： `POST` / `PATCH` / `DELETE` 需附带 CSRF Token（蓝图级别 `csrf.exempt(api_bp)` 全局豁免 API，页面路由通过 `base.html` 中的 `<meta name="csrf-token">` 注入）
- **所有 API 接口默认需要登录**（`@login_required`），以下标注"无需登录"的除外

---

### 4.2 认证 API

#### 4.2.1 登录页面

```
GET /login
```

> 无需登录

返回 `login.html`（独立页面，不继承 `base.html`）。

若已登录则 302 重定向到 `/`。支持 `?error=xxx` 和 `?expired=1` 参数展示错误提示。

#### 4.2.2 提交登录

```
POST /login
Content-Type: application/x-www-form-urlencoded
```

> 无需登录

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

成功：302 重定向到 `/`。失败：302 重定向到 `/login?error=用户名或密码错误`。

#### 4.2.3 登出

```
GET /logout
```

清除 session，302 重定向到 `/login`。

#### 4.2.4 修改密码

```
POST /api/change-password
Content-Type: application/json
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| old_password | string | 是 | 旧密码 |
| new_password | string | 是 | 新密码（最少 6 位） |

**响应**：

```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": {}
}
```

#### 4.2.5 获取当前用户信息

```
GET /api/me
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "username": "admin",
    "display_name": "管理员",
    "role": "admin"
  }
}
```

---

### 4.3 台账 CRUD API

#### 4.3.1 新增台账

```
POST /api/ledger
Content-Type: application/json
```

| 参数名 | 类型 | 必填 | 校验 |
|--------|------|------|------|
| serial_no | string | 否 | ≤ 30 字符，不填则自动生成 |
| duty_officer | string | 是 | 1~50 字符 |
| personnel | string | 否 | ≤ 2000 字符 |
| incident_time | string | 是 | `YYYY-MM-DD HH:MM` 格式 |
| info_source | string | 是 | 枚举值（见 enum_config 表） |
| jurisdiction | string | 是 | 1~100 字符 |
| case_category | string | 是 | 枚举值（见 enum_config 表） |
| case_basic_info | string | 是 | 1~5000 字符 |
| on_site_personnel | string | 否 | ≤ 200 字符 |
| on_site_handling | string | 否 | ≤ 5000 字符 |
| analysis_situation | string | 否 | ≤ 5000 字符 |
| closure_status | string | 否 | ≤ 3000 字符 |
| follow_up | string | 否 | ≤ 3000 字符 |
| handover | string | 否 | ≤ 2000 字符 |
| is_closed | int | 否 | 0 或 1，默认 0 |
| is_escalated | int | 否 | 0 或 1，默认 0 |

文件上传单独接口（见 4.4），不与此接口混合。

**响应**：

```json
{
  "code": 200,
  "message": "台账登记成功",
  "data": {
    "id": 1,
    "serial_no": "JQ-20260715-0001"
  }
}
```

#### 4.3.2 编辑台账

```
PATCH /api/ledger/<id>
Content-Type: application/json
```

使用 PATCH 表示部分更新，未传的字段保持原值不变（不会置为 NULL）。

参数同新增接口，全部可选。

#### 4.3.3 删除台账（软删除）

```
DELETE /api/ledger/<id>
```

将 `is_deleted` 置为 1，不物理删除数据。图片文件不立即删除（保留以便恢复）。

#### 4.3.4 台账详情

```
GET /api/ledger/<id>
```

默认包含关联的图片列表。若记录已被软删除（`is_deleted=1`），返回 404。

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "serial_no": "JQ-20260715-0001",
    "duty_officer": "张三",
    "personnel": "李四、王五",
    "incident_time": "2026-07-15 14:30",
    "info_source": "110接报",
    "jurisdiction": "XX派出所辖区",
    "case_category": "治安",
    "case_basic_info": "...",
    "on_site_personnel": "民警甲、民警乙",
    "on_site_handling": "...",
    "analysis_situation": "...",
    "closure_status": "...",
    "follow_up": "...",
    "handover": "...",
    "is_closed": 0,
    "is_escalated": 0,
    "is_deleted": 0,
    "created_at": "2026-07-15 15:00:00",
    "updated_at": "2026-07-15 15:00:00",
    "images": [
      {
        "id": 1,
        "uuid_filename": "a1b2c3d4.jpg",
        "original_name": "现场照片.jpg",
        "file_size": 204800,
        "sort_order": 0,
        "url": "/api/ledger/1/images/a1b2c3d4.jpg"
      }
    ]
  }
}
```

#### 4.3.5 台账列表（分页 + 筛选 + 排序）

```
GET /api/ledger?page=1&per_page=20&keyword=&jurisdiction=&case_category=&is_closed=&is_escalated=&date_from=&date_to=&sort_by=&sort_order=
```

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| per_page | int | 否 | 20 | 每页条数（最大 100） |
| keyword | string | 否 | — | 模糊匹配 serial_no、personnel、case_basic_info |
| jurisdiction | string | 否 | — | 辖区精确匹配 |
| case_category | string | 否 | — | 类别精确匹配 |
| is_closed | int | 否 | — | 办结状态 |
| is_escalated | int | 否 | — | 提级状态 |
| date_from | string | 否 | — | 发生时间起始 `YYYY-MM-DD` |
| date_to | string | 否 | — | 发生时间截止 `YYYY-MM-DD` |
| sort_by | string | 否 | incident_time | 排序字段（白名单：incident_time / created_at / serial_no） |
| sort_order | string | 否 | desc | asc 或 desc |

列表返回字段子集（不含大文本和图片详情），减少传输量：

| 字段 | 说明 |
|------|------|
| id, serial_no, duty_officer, incident_time, jurisdiction, case_category, info_source | 列表基本字段 |
| case_basic_info_summary | 基本情况摘要（前 80 字 + "..."） |
| is_closed, is_escalated | 状态标记 |
| image_count | 图片数量 |
| created_at, updated_at | 时间戳 |

#### 4.3.6 获取下一个序号

```
GET /api/ledger/next-serial
```

前端在进入新建页面时调用此接口，获取自动生成的序号并预填到表单中。序号不锁定，如果提交时已被占用返回 409 冲突。

---

### 4.4 图片管理 API

#### 4.4.1 上传图片

```
POST /api/ledger/<id>/images
Content-Type: multipart/form-data
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| files | file[] | 是 | 图片文件，允许多文件一次上传 |

**新增时流程**：先 `POST /api/ledger` 获取 ID → 再 `POST /api/ledger/<id>/images`。

**安全约束**：MIME 白名单 `image/jpeg, image/png, image/gif, image/webp`，单文件最大 10MB，UUID 重命名存储于 `uploads/`。

#### 4.4.2 预览/下载图片

```
GET /api/ledger/<id>/images/<uuid_filename>
```

通过 API 层返回图片流，不走静态文件直接访问。

#### 4.4.3 删除图片

```
DELETE /api/ledger/<id>/images/<image_id>
```

从数据库和文件系统同时移除。

---

### 4.5 枚举配置 API

```
GET /api/enums
```

从 `enum_config` 表动态读取已启用项，同时从 `work_ledger` 表 `SELECT DISTINCT jurisdiction` 汇总辖区值。

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "info_source": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"}
    ],
    "case_category": [
      {"value": "刑事", "label": "刑事"},
      {"value": "治安", "label": "治安"}
    ],
    "jurisdiction": [
      {"value": "XX派出所辖区", "label": "XX派出所辖区"}
    ]
  }
}
```

---

## 五、关键业务逻辑设计

### 5.1 序号自动生成（并发安全）

格式 `JQ-{YYYYMMDD}-{NNNN}`，NNNN 为当天计数器（4 位，从 0001 开始）。

使用 MySQL 行级锁 `SELECT ... FOR UPDATE` 保证并发安全，调用方遇到 UNIQUE 冲突时自动重试（最多 3 次）。

```python
def generate_serial_no():
    today_prefix = f"JQ-{datetime.now():%Y%m%d}-"

    last = (
        WorkLedger.query
        .filter(WorkLedger.serial_no.like(f"{today_prefix}%"))
        .order_by(WorkLedger.serial_no.desc())
        .with_for_update()
        .first()
    )
    if last:
        last_num = int(last.serial_no.split("-")[-1])
        next_num = last_num + 1
    else:
        next_num = 1

    return f"{today_prefix}{next_num:04d}"
```

### 5.2 图片上传的事务一致性

```
1. 接收上传文件 → 校验类型/大小
2. 暂存到 uploads/tmp/{uuid}.ext
3. 写入 ledger_images 记录（数据库事务）
4. 事务提交成功 → 将文件从 uploads/tmp/ 移动到 uploads/
5. 事务提交失败 → 清理 uploads/tmp/ 中的临时文件
```

### 5.3 软删除实现

所有默认查询过滤 `is_deleted=0`，详情/编辑/删除操作前检查软删除状态（返回 404）。

### 5.4 关键字搜索

keyword 参数使用 `OR LIKE` 匹配 `serial_no`、`personnel`、`case_basic_info` 三个字段。

> 未来扩展：数据量超过 10 万条时可用 MySQL 全文索引替代 LIKE。

### 5.5 认证流程

```
用户访问任何页面 → @login_required 拦截 → session 无 user_id
    → API 请求：返回 {"code": 401}
    → 页面请求：302 重定向到 /login
→ POST /login → 校验成功 → session["user_id"] = user.id + session.permanent = True
    → 302 重定向到 /
```

登录时设置 `session.permanent = True`，配合 `PERMANENT_SESSION_LIFETIME = timedelta(hours=24)` 实现 24 小时无操作自动过期。

### 5.6 用户管理

不提供前端注册入口。通过 CLI 脚本 `manage.py` 管理：

```bash
python manage.py create-user <username> <password> <display_name> [admin|user]
python manage.py reset-password <username> <new_password>
python manage.py list-users
python manage.py disable-user <username>
python manage.py enable-user <username>
```

`manage.py` 使用轻量 Flask 实例（`Flask(__name__) + db.init_app(app)`），不启动完整 Web 应用。

---

## 六、配置设计

### 6.1 多环境配置

```python
# config.py

import os
from datetime import timedelta

BASE_DIR = os.path.abspath(os.path.dirname(__file__))
MYSQL_BASE = "mysql+pymysql://root:root@localhost:3306"


class Config:
    SECRET_KEY = "change-me-in-production"
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    MAX_CONTENT_LENGTH = 10 * 1024 * 1024

    SESSION_COOKIE_HTTPONLY = True
    SESSION_COOKIE_SAMESITE = "Lax"
    PERMANENT_SESSION_LIFETIME = timedelta(hours=24)

    UPLOAD_FOLDER = os.path.join(BASE_DIR, "uploads")
    UPLOAD_TMP_FOLDER = os.path.join(UPLOAD_FOLDER, "tmp")
    ALLOWED_IMAGE_TYPES = {"image/jpeg", "image/png", "image/gif", "image/webp"}
    SINGLE_IMAGE_MAX_SIZE = 10 * 1024 * 1024


class DevelopmentConfig(Config):
    DEBUG = True
    SQLALCHEMY_DATABASE_URI = f"{MYSQL_BASE}/work_ledger_dev?charset=utf8mb4"


class ProductionConfig(Config):
    DEBUG = False
    SQLALCHEMY_DATABASE_URI = f"{MYSQL_BASE}/work_ledger?charset=utf8mb4"


config_map = {
    "development": DevelopmentConfig,
    "production": ProductionConfig,
}
```

### 6.2 依赖清单

```
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-WTF==1.2.1
marshmallow==3.20.1
PyMySQL==1.1.0
Werkzeug==3.0.1
```

---

## 七、项目目录结构

```
backend/
├── run.py                         # 应用启动入口
├── config.py                      # 多环境配置（MySQL + Session）
├── requirements.txt               # Python 依赖清单
├── wsgi.py                        # 生产 WSGI 入口
├── manage.py                      # 用户管理 CLI 脚本
│
├── app/                           # 应用主目录
│   ├── __init__.py                # Flask 应用工厂、MySQL 自动建库、全局异常注册、认证注入
│   │
│   ├── models/                    # 数据模型层
│   │   ├── __init__.py
│   │   ├── base.py                # 基类（to_dict 序列化）
│   │   ├── ledger.py              # WorkLedger 模型（含 to_list_item / to_detail）
│   │   ├── ledger_image.py        # LedgerImage 模型（UUID 图片存储）
│   │   ├── enum_config.py         # EnumConfig 模型（code/label 分离 + 自动播种）
│   │   └── user.py                # User 模型（密码哈希 + init_default_admin）
│   │
│   ├── routes/                    # 路由层
│   │   ├── __init__.py
│   │   ├── auth.py                # 认证路由（登录/登出页面）
│   │   ├── main.py                # 页面路由（列表/详情页，全部 @login_required）
│   │   └── api.py                 # RESTful API 路由（CRUD + 图片 + 枚举 + 改密/me）
│   │
│   ├── services/                  # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── serial_service.py      # 序号生成（JQ-YYYYMMDD-NNNN）
│   │   ├── ledger_service.py      # 台账 CRUD + 分页筛选排序 + 软删除
│   │   ├── image_service.py       # 图片上传/删除/预览（暂存→验证→提交→移动）
│   │   └── enum_service.py        # 枚举查询 + 辖区动态 DISTINCT
│   │
│   ├── schemas/                   # Marshmallow Schema
│   │   ├── __init__.py
│   │   └── ledger_schema.py       # 台账创建/更新/列表查询 Schema
│   │
│   ├── errors/                    # 异常定义
│   │   ├── __init__.py            # 全局错误处理器注册
│   │   └── exceptions.py          # NotFoundException、ConflictException、ValidationException
│   │
│   ├── utils/                     # 工具函数
│   │   ├── __init__.py
│   │   ├── helpers.py             # 日期格式化、文本截断
│   │   ├── upload.py              # 文件校验、UUID 命名、暂存/移动/清理
│   │   ├── response.py            # 统一 JSON 响应格式
│   │   └── auth.py                # login_required / admin_required 装饰器
│   │
│   ├── templates/                 # Jinja2 模板
│   │   ├── login.html             # 登录页面（独立，不继承 base）
│   │   ├── base.html              # 基础布局（含导航栏用户信息 + 修改密码弹窗）
│   │   ├── index.html             # 台账列表页
│   │   ├── ledger_form.html       # 新增/编辑台账页
│   │   └── ledger_detail.html     # 台账详情页
│   │
│   └── static/                    # 静态资源
│       ├── css/
│       │   └── style.css          # 自定义样式
│       └── js/
│           └── main.js            # Fetch API 封装 + Toast + 401 拦截 + changePassword
│
├── migrations/                    # Flask-Migrate 迁移文件（自动生成）
│
├── uploads/                       # 图片存储目录
│   └── tmp/                       # 上传暂存目录
│
└── instance/                      # 实例文件夹（预留）
```

---

## 八、应用工厂函数

```python
# app/__init__.py

from flask import Flask
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate
from flask_wtf.csrf import CSRFProtect

db = SQLAlchemy()
migrate = Migrate()
csrf = CSRFProtect()


def _ensure_mysql_database(db_uri):
    """MySQL 启动时自动建库"""
    import pymysql, re

    match = re.match(r"mysql\+pymysql://(.+):(.+)@(.+):(\d+)/([^?]+)", db_uri)
    if not match:
        return

    user, password, host, port, db_name = match.groups()
    conn = pymysql.connect(
        host=host, port=int(port), user=user, password=password, charset="utf8mb4"
    )
    try:
        with conn.cursor() as cursor:
            cursor.execute(
                f"CREATE DATABASE IF NOT EXISTS `{db_name}` "
                f"DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci"
            )
        conn.commit()
    finally:
        conn.close()


def create_app(config_name="development"):
    import os
    from config import config_map

    app = Flask(__name__)
    app.config.from_object(config_map[config_name])

    _ensure_mysql_database(app.config["SQLALCHEMY_DATABASE_URI"])

    db.init_app(app)
    migrate.init_app(app, db)
    csrf.init_app(app)

    from app.routes.auth import auth_bp
    from app.routes.main import main_bp
    from app.routes.api import api_bp

    app.register_blueprint(auth_bp)
    app.register_blueprint(main_bp)
    app.register_blueprint(api_bp, url_prefix="/api")

    from app.errors import register_error_handlers
    register_error_handlers(app)

    @app.context_processor
    def inject_current_user():
        from flask import g, session
        from app.models.user import User

        if hasattr(g, 'current_user'):
            return {"current_user": g.current_user}

        uid = session.get("user_id")
        if uid:
            user = User.query.get(uid)
            if user and user.is_active:
                g.current_user = user
                return {"current_user": user}
        return {}

    os.makedirs(app.config["UPLOAD_FOLDER"], exist_ok=True)
    os.makedirs(app.config["UPLOAD_TMP_FOLDER"], exist_ok=True)

    with app.app_context():
        db.create_all()
        from app.models.enum_config import EnumConfig
        EnumConfig.init_defaults()
        from app.models.user import User
        User.init_default_admin()

    return app
```

---

## 九、部署说明

### 9.1 环境准备

```bash
# 1. 确保 MySQL 5.7+ 已启动（localhost:3306, 用户 root/root）
# 2. 创建虚拟环境并安装依赖
python -m venv venv
venv\Scripts\activate            # Windows
pip install -r requirements.txt

# 3. （可选）初始化 Flask-Migrate
flask db init
flask db migrate -m "init"
flask db upgrade

# 4. 启动应用（首次启动自动建库建表 + 播种默认数据）
python run.py
```

### 9.2 启动应用

```bash
# 开发模式（MySQL 数据库 work_ledger_dev）
set FLASK_ENV=development
python run.py

# 生产模式（MySQL 数据库 work_ledger）
set FLASK_ENV=production
python run.py
```

启动后访问 `http://localhost:5000`，跳转登录页，默认账号 `admin` / `admin123`。

### 9.3 用户管理

```bash
# 创建用户
python manage.py create-user zhangsan pass1234 张三 user

# 查看所有用户
python manage.py list-users

# 重置密码 / 禁用 / 启用
python manage.py reset-password zhangsan newpass5678
python manage.py disable-user zhangsan
python manage.py enable-user zhangsan
```

### 9.4 生产部署

- 使用 `waitress`（Windows）或 `gunicorn`（Linux）作为 WSGI 服务器
- MySQL 支持多 worker 并发写（与 SQLite 不同，不再受单 worker 限制）
- 使用 Nginx 做反向代理和静态文件服务
- 设置 `SECRET_KEY` 为随机强密码
- 定期备份 MySQL 数据库和 `uploads/` 目录

### 9.5 生产 WSGI 入口

```python
# wsgi.py
import os
from app import create_app
config_name = os.environ.get("FLASK_ENV", "production")
app = create_app(config_name)
```

```bash
# Windows (waitress)
waitress-serve --port=5000 wsgi:app

# Linux (gunicorn)
gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
```
