# 警情案件工作台账登记系统 - 前端设计文档

## 一、技术方案

### 1.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 模板引擎 | Jinja2（Flask 内置） | 服务端渲染页面骨架 + 初始数据注入 |
| CSS 框架 | Bootstrap 5.3 | 响应式布局、表单、模态框、Toast |
| 图标库 | Bootstrap Icons | Bootstrap 官方图标集，无需额外依赖 |
| JS 框架 | Alpine.js 3.x | 轻量响应式框架，处理表单状态、列表筛选、弹窗切换 |
| HTTP 请求 | Fetch API | 异步 CRUD 与文件上传 |
| 文件上传 | 原生 input[type=file] + FormData | 多选、预览、拖拽、进度 |

**为什么不继续用纯原生 JS：**

原设计文档要求"原生 JavaScript + Fetch API"，但本系统涉及以下复杂前端交互：

- Bootstrap Accordion 多面板表单的状态联动
- 图片拖拽/多选/预览/删除的全流程管理
- 表单序列化（FormData 构建）与校验
- Toast / Modal 的生命周期管理
- 筛选条件记忆到 URL query string
- beforeunload 未保存提示

Bootstrap 5 的 Modal、Toast、Collapse 已不再依赖 jQuery，但需要手动 `new bootstrap.Modal(...)` 等 JS API 调用。Alpine.js 体积极小（约 15KB gzipped），没有虚拟 DOM，直接在 HTML 上声明式绑定状态，完美契合"轻量、无构建工具"的约束，同时能大幅减少手写 DOM 操作代码。

> **与总设计文档的差异说明**：总设计文档（v1.0）中前端交互描述为"原生 JavaScript + Fetch API"，本设计在评估实际交互复杂度（多面板表单、图片拖拽管理、Toast/Modal 生命周期等）后升级为 Alpine.js。建议总设计文档技术栈表同步更新。

### 1.2 核心依赖清单（CDN 引入）

```html
<!-- Bootstrap 5 CSS -->
<link href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">

<!-- Bootstrap Icons -->
<link href="https://cdn.bootcdn.net/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">

<!-- Alpine.js -->
<script src="https://cdn.bootcdn.net/ajax/libs/alpinejs/3.14.1/cdn.min.js" defer></script>

<!-- Bootstrap 5 JS Bundle (含 Popper) -->
<script src="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.3.3/js/bootstrap.bundle.min.js"></script>
```

### 1.3 自定义静态文件

| 文件 | 职责 |
|------|------|
| `static/css/style.css` | 覆盖 Bootstrap 变量、自定义组件样式、响应式适配 |
| `static/js/utils.js` | 通用工具函数（日期格式化、文件大小、防抖节流等） |
| `static/js/api.js` | 所有 Fetch API 封装（统一错误处理、CSRF token 注入） |
| `static/js/components.js` | 复用的 UI 组件逻辑（Toast、Confirm Modal、图片预览） |

---

## 二、页面路由与模板规划

### 2.1 路由表

| 路由 | 页面名称 | 对应模板 |
|------|---------|---------|
| `/` | 台账列表页 | `index.html` |
| `/ledger/add` | 新增台账页 | `ledger_form.html` |
| `/ledger/<id>/edit` | 编辑台账页 | `ledger_form.html`（共用） |
| `/ledger/<id>/view` | 台账详情页 | `ledger_detail.html` |

### 2.2 模板继承结构

```
base.html                        # 公共布局：导航栏、页脚、CDN 引入、CSRF meta
├── index.html                   # 继承 base，填充列表内容
├── ledger_form.html             # 继承 base，填充表单内容
└── ledger_detail.html           # 继承 base，填充详情内容
```

### 2.3 base.html 公共区块

```jinja2
{# base.html — 必备要素 #}
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {# CSRF Token（Flask-WTF 方式注入） #}
  <meta name="csrf-token" content="{{ csrf_token() }}">
  <title>{% block title %}警情案件工作台账登记系统{% endblock %}</title>
  <!-- Bootstrap + Bootstrap Icons CSS -->
  <!-- 自定义 style.css -->
  {% block extra_css %}{% endblock %}
</head>
<body>
  <!-- 顶部导航栏 -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">...</nav>

  <!-- 主内容区域 -->
  <main class="container-fluid py-3">
    {% block content %}{% endblock %}
  </main>

  <!-- 全局 Toast 容器 -->
  <div class="toast-container position-fixed bottom-0 end-0 p-3" id="globalToastContainer"></div>

  <!-- Alpine.js + Bootstrap JS -->
  <!-- utils.js + api.js + components.js -->
  {% block extra_js %}{% endblock %}
</body>
</html>
```

---

## 三、API 交互规范

### 3.1 统一 Fetch 封装（api.js）

所有请求通过统一的 `api` 对象发出，集中处理 CSRF token、JSON 解析、错误判断、超时。

```javascript
// api.js

const API_BASE = '';

const api = {
  /**
   * 通用请求
   * @param {string} url
   * @param {object} options - fetch 选项
   * @param {number} timeout - 超时毫秒，默认 30000
   * @returns {Promise<{code: number, message: string, data: any}>}
   */
  async request(url, options = {}, timeout = 30000) {
    const controller = new AbortController();
    const timer = setTimeout(() => controller.abort(), timeout);

    const headers = {
      'X-CSRFToken': document.querySelector('meta[name="csrf-token"]')?.content || '',
      ...options.headers
    };

    // 非 FormData 时自动设 Content-Type
    if (!(options.body instanceof FormData)) {
      headers['Content-Type'] = 'application/json';
    }

    try {
      const res = await fetch(API_BASE + url, {
        ...options,
        headers,
        signal: controller.signal
      });
      clearTimeout(timer);

      const json = await res.json();
      if (json.code !== 200) {
        throw new ApiError(json.code, json.message);
      }
      return json;
    } catch (err) {
      clearTimeout(timer);
      if (err.name === 'AbortError') {
        throw new ApiError(0, '请求超时，请稍后重试');
      }
      if (err instanceof ApiError) throw err;
      throw new ApiError(0, '网络异常，请检查网络连接');
    }
  },

  get(url, params = {}) {
    const qs = new URLSearchParams(params).toString();
    return this.request(url + (qs ? '?' + qs : ''));
  },

  post(url, data) {
    return this.request(url, { method: 'POST', body: data });
  },

  patch(url, data) {
    return this.request(url, { method: 'PATCH', body: data });
  },

  delete(url) {
    return this.request(url, { method: 'DELETE' });
  },

  postFormData(url, file, fieldName = 'file') {
    const formData = new FormData();
    formData.append(fieldName, file);
    return this.request(url, { method: 'POST', body: formData });
  }
};

class ApiError extends Error {
  constructor(code, message) {
    super(message);
    this.code = code;
    this.name = 'ApiError';
  }
}
```

### 3.2 图片上传策略

图片使用**独立接口**进行上传和删除，与台账表单提交分离。

**后端提供的图片接口：**

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/ledger/<id>/images` | POST | 上传图片到指定台账（FormData，字段名 `file`） |
| `/api/ledger/<id>/images/<image_id>` | DELETE | 删除指定台账中的某张图片 |

**前端流程：**

**新增模式：**
1. 用户填写表单 → 点击"提交保存"
2. 前端先调用 `POST /api/ledger` 创建台账（纯 JSON，不含图片）
3. 创建成功后获取 `id`
4. 若有新图片，逐一调用 `POST /api/ledger/<id>/images` 上传
5. 全部完成后 Toast 提示成功 → 跳转列表页

**编辑模式：**
1. 页面加载时，通过 `GET /api/ledger/<id>` 获取已有图片列表（`analysis_images` 数组）
2. 用户可删除已有图片 → 调用 `DELETE /api/ledger/<id>/images/<image_id>`，即时生效
3. 用户可选择新图片 → 提交表单后调用 `POST /api/ledger/<id>/images` 上传
4. 文本字段修改走 `PATCH /api/ledger/<id>`（纯 JSON，不含文件）

**前端维护的状态：**

```javascript
// 编辑模式下图片管理
{
  existingImages: [],      // 从详情接口获取的已有图片 [{id, url}]
  newImageFiles: []        // 用户新选择的 File 对象列表
}
```

- 点击删除已有图片 → 调用 DELETE 接口 → 成功后从 `existingImages` 中移除
- 选择新文件 → 加入 `newImageFiles`，显示缩略图预览
- 提交表单时：
  1. 先 `PATCH /api/ledger/<id>` 更新文本字段
  2. 再逐一 `POST /api/ledger/<id>/images` 上传新图片
- 用户点击"取消"或关闭页面 → 已有图片不受影响（删除操作已实时生效）

### 3.3 枚举接口约定

`GET /api/enums` 返回结构约定如下，供前端填充下拉框：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "info_source": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"},
      {"value": "上级交办", "label": "上级交办"},
      {"value": "其他部门移交", "label": "其他部门移交"}
    ],
    "case_category": [
      {"value": "刑事", "label": "刑事"},
      {"value": "治安", "label": "治安"},
      {"value": "交通", "label": "交通"},
      {"value": "求助", "label": "求助"}
    ]
  }
}
```

**下拉框统一使用 `{value, label}` 结构**。前端在页面加载时调用一次该接口，将结果缓存到 Alpine.js 全局状态中，列表页和表单页共享。

---

## 四、台账列表页（index.html）

### 4.1 页面布局

```
┌─────────────────────────────────────────────────────┐
│  导航栏                                              │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │  搜索筛选栏（默认展开，可折叠）                 │    │
│  │  [关键词...] [辖区▾] [类别▾] [办结▾] [提级▾]   │    │
│  │  [日期从...] ~ [日期至...]                    │    │
│  │  [🔍 搜索] [↺ 重置]  [➕ 新增台账]             │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │  统计摘要行（可选）                            │    │
│  │  共 X 条 | 未办结 Y | 已提级 Z                │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │  数据表格（固定表头、水平可滚动）               │    │
│  │  序号 │ 直守人员 │ 发生时间 │ 辖区 │ ...       │    │
│  │  ─────────────────────────────────────       │    │
│  │  ...                                         │    │
│  │           或：暂无匹配记录                     │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │  分页导航                                     │    │
│  │  « 上一页   [1] [2] [3] ... [8]   下一页 »    │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

### 4.2 状态定义（Alpine.js）

```javascript
// index.html 中的 Alpine 组件

x-data="{
  // --- 筛选状态（初始化自 URL query string） ---
  keyword: '',
  jurisdiction: '',
  case_category: '',
  is_closed: '',
  is_escalated: '',
  date_from: '',
  date_to: '',

  // --- 分页状态 ---
  page: 1,
  per_page: 20,
  total: 0,
  total_pages: 0,
  items: [],

  // --- 排序 ---
  sort_by: 'incident_time',
  sort_order: 'desc',

  // --- UI 状态 ---
  filterExpanded: true,
  loading: false,
  empty: false,

  // --- 枚举数据 ---
  jurisdictions: [],      // 辖区列表（后端统计后返回）
  caseCategories: [],

  // --- 生命周期 ---
  init() {
    this.loadEnums();
    this.loadList();
  },

  search() { this.page = 1; this.loadList(); },
  reset() { /* 清空所有筛选，page=1，重新加载 */ },
  goPage(p) { this.page = p; this.loadList(); },
  loadList() { /* 调用 api.get('/api/ledger', params)，更新 items/total，params 含 sort_by/sort_order */ },
}"
```

### 4.3 数据表格列定义

重新调整列宽，总计约 1000px，适配 1366px 屏幕且留出边距：

| 列名 | 宽度 | 说明 |
|------|------|------|
| 序号 | 130px | serial_no，可点击跳转详情 |
| 直守人员 | 70px | duty_officer |
| 发生时间 | 120px | incident_time，格式 YYYY-MM-DD HH:mm |
| 辖区 | 100px | jurisdiction |
| 案件类别 | 80px | case_category |
| 信息来源 | 100px | info_source |
| 基本情况 | 180px | case_basic_info 截断，hover 显示 tooltip |
| 状态 | 100px | 两个标签：`已办结`(绿) / `未办结`(橙)；`已提级`(红) / `-` |
| 操作 | 120px | 查看 / 编辑 / 删除 按钮 |

合计约 1000px。在 Bootstrap `table-responsive` 包装下，窄屏自动出现水平滚动条。操作列使用 `position: sticky; right: 0` 固定在右侧。

**排序支持**：表头中"序号""发生时间"两列支持点击排序（升序/降序切换）。点击表头时更新 `sort_by` 和 `sort_order`，重新请求列表数据。`sort_by` 和 `sort_order` 作为查询参数传给后端 `GET /api/ledger`。

### 4.4 筛选条件持久化

- 每次 `search()` 执行时，将当前筛选条件写入 `window.history.replaceState` 更新 URL query string
- 页面初始化时从 `URLSearchParams` 读取初始筛选值
- 这样用户刷新页面或分享链接时筛选条件不丢失

### 4.5 删除确认弹窗

使用 Bootstrap Modal，Alpine.js 控制：

```html
<!-- 确认删除弹窗 -->
<div class="modal fade" x-ref="deleteModal" tabindex="-1">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">确认删除</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        确定要删除序号为 <strong x-text="deleteTargetSerial"></strong> 的台账记录吗？此操作不可撤销。
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
        <button type="button" class="btn btn-danger" @click="doDelete">确认删除</button>
      </div>
    </div>
  </div>
</div>
```

---

## 五、新增/编辑台账页（ledger_form.html）

### 5.1 整体布局

```
┌─────────────────────────────────────────────────────┐
│  导航栏                                              │
├─────────────────────────────────────────────────────┤
│  面包屑：台账列表 > 新增台账 / 编辑台账               │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ ▼ 面板一：基本信息（默认展开）★ 必填项       │    │
│  │   序号(自填/自动)  直守人员★  涉及人员       │    │
│  │   发生时间★         信息来源★   辖区★         │    │
│  │   案件类别★                                  │    │
│  │   案件基本情况★（textarea 4行）              │    │
│  ├─────────────────────────────────────────────┤    │
│  │ ▶ 面板二：现场处置                          │    │
│  │   现场处置人员  现场开展处置情况（textarea） │    │
│  ├─────────────────────────────────────────────┤    │
│  │ ▶ 面板三：研判分析                          │    │
│  │   研判情况（textarea 4行）                   │    │
│  │   研判图片上传（拖拽/选择 + 预览 + 删除）     │    │
│  ├─────────────────────────────────────────────┤    │
│  │ ▶ 面板四：闭环与交接                        │    │
│  │   闭环情况  后续移交跟进  人员交接            │    │
│  │   是否办结 [toggle]  是否提级 [toggle]        │    │
│  └─────────────────────────────────────────────┘    │
│                                                      │
│  [← 取消返回]  [💾 提交保存]                          │
└─────────────────────────────────────────────────────┘
```

### 5.2 状态定义（Alpine.js）

```javascript
x-data="{
  // --- 模式 ---
  mode: 'add',             // 'add' | 'edit'
  recordId: null,          // 编辑时由后端注入

  // --- 表单字段 ---
  serial_no: '',
  duty_officer: '',
  personnel: '',
  incident_time: '',
  info_source: '',
  jurisdiction: '',
  case_category: '',
  case_basic_info: '',
  on_site_personnel: '',
  on_site_handling: '',
  analysis_situation: '',
  closure_status: '',
  follow_up: '',
  handover: '',
  is_closed: false,
  is_escalated: false,

  // --- 图片管理 ---
  existingImages: [],      // [{id, url}]
  newImageFiles: [],       // 新选择的 File 对象
  uploading: false,
  uploadProgress: 0,

  // --- 枚举 ---
  infoSources: [],
  caseCategories: [],

  // --- 校验 ---
  errors: {},              // { fieldName: '错误信息' }
  dirty: false,            // 表单是否有未保存修改
  submitting: false,

  // --- 生命周期 ---
  init() {
    this.loadEnums();
    if (this.mode === 'edit') {
      this.loadRecord();
    }
    // beforeunload 监听
    window.addEventListener('beforeunload', (e) => {
      if (this.dirty) e.preventDefault();
    });
  },

  // --- 方法 ---
  loadRecord() { /* api.get('/api/ledger/' + id), 回填表单 */ },
  addImages(e) { /* 处理文件选择，生成临时预览 URL */ },
  deleteExistingImage(imageId) { /* 调用 api.delete('/api/ledger/' + id + '/images/' + imageId) */ },
  removeNewImage(index) { /* 从 newImageFiles 中移除，释放 ObjectURL */ },
  validate() { /* 返回 true/false，同时更新 errors */ },
  submit() { /* 校验 → 先提交表单 JSON → 再上传新图片 → 跳转列表 */ },
  cancel() { /* 检查 dirty → 提示确认 → 返回列表 */ }
}"
```

### 5.3 字段校验规则

| 字段 | 必填 | 规则 |
|------|------|------|
| serial_no | 否 | 1-30 字符，仅允许 中文/字母/数字/连字符 |
| duty_officer | **是** | 1-50 字符 |
| incident_time | **是** | 不能为空，不能是未来时间，格式 YYYY-MM-DD HH:mm |
| info_source | **是** | 必须从下拉列表中选择 |
| jurisdiction | **是** | 1-100 字符 |
| case_category | **是** | 必须从下拉列表中选择 |
| case_basic_info | **是** | 1-5000 字符 |
| 其他文本域 | 否 | 0-2000 字符 |

**校验时机**：提交时全量校验 + 失焦时显示单字段错误（如果之前已验证过）。

### 5.4 图片上传规范

| 约束项 | 值 |
|--------|-----|
| 允许类型 | image/jpeg, image/png, image/gif, image/webp |
| 单文件上限 | 10 MB |
| 最大数量 | 9 张（与 existing 合并计算） |
| 文件名校验 | 前端不校验文件名，由后端 UUID 重命名 |

**UI 交互流程：**

1. 点击上传区域或拖拽文件 → 前端校验类型+大小 → 生成 `URL.createObjectURL` 缩略图
2. 已有图片网格展示 → 每张图右上角有删除按钮 → 点击后调用 DELETE 接口 → 成功后从预览区移除
3. 新选图片网格展示 → 每张图右上角有删除按钮 → 点击后从 `newImageFiles` 移除，释放 ObjectURL
4. 表单提交 → 先提交 JSON 文本字段 → 获取 id → 再逐张 POST 上传新图片

### 5.5 表单控件类型确认

| 字段 | 控件 | 说明 |
|------|------|------|
| serial_no | `<input type="text">` | 右侧有"自动生成"按钮 |
| duty_officer | `<input type="text">` | |
| personnel | `<input type="text">` | 支持逗号分隔多人 |
| incident_time | `<input type="datetime-local">` | step=60（精确到分钟） |
| info_source | `<select>` | 下拉选择 |
| jurisdiction | `<input type="text">` + `<datalist>` | 支持输入 + 常用辖区建议 |
| case_category | `<select>` | 下拉选择 |
| case_basic_info | `<textarea rows="4">` | |
| on_site_personnel | `<input type="text">` | |
| on_site_handling | `<textarea rows="4">` | |
| analysis_situation | `<textarea rows="4">` | |
| closure_status | `<textarea rows="3">` | |
| follow_up | `<textarea rows="3">` | |
| handover | `<textarea rows="3">` | |
| is_closed | Bootstrap form-switch | 开关样式 `.form-check.form-switch` |
| is_escalated | Bootstrap form-switch | 开关样式 `.form-check.form-switch` |

### 5.6 提交流程

```
用户点击"提交保存"
  → 按钮 disabled + 显示 spinner
  → 前端校验（validate）
    → 不通过：显示错误提示，滚动到第一个错误字段，恢复按钮
    → 通过：继续
  → 构建 JSON body：
      - 文本字段逐一加入
      - is_closed/is_escalated 转为布尔值
  → 新增模式：api.post('/api/ledger', jsonBody)
    编辑模式：api.patch('/api/ledger/' + id, jsonBody)
    → 成功：获取 id（新增模式），继续上传图片
    → 失败 400：显示后端返回的错误信息，恢复按钮
    → 失败 0/500：Toast 提示"保存失败"，保留表单数据
  → 若有 newImageFiles：
      逐一 api.postFormData('/api/ledger/' + id + '/images', file)
    → 全部完成后 Toast 提示成功 → 跳转列表页
```

---

## 六、台账详情页（ledger_detail.html）

### 6.1 页面布局

```
┌─────────────────────────────────────────────────────┐
│  导航栏                                              │
├─────────────────────────────────────────────────────┤
│  面包屑：台账列表 > 台账详情                          │
├─────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────┐       │
│  │ 📋 基本信息                               │       │
│  │ 序号：JQ-20260715-001    直守人员：张三    │       │
│  │ 涉及人员：李四、王五      发生时间：...     │       │
│  │ 信息来源：110接报         辖区：XX派出所    │       │
│  │ 案件类别：治安            ┌状态标签────────┐│       │
│  │ 基本情况：...             │ 未办结 │ 未提级 ││       │
│  └──────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────┐       │
│  │ 🚔 处置信息                               │       │
│  │ 现场处置人员：民警甲、民警乙               │       │
│  │ 处置情况：...                              │       │
│  └──────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────┐       │
│  │ 🔍 研判分析                               │       │
│  │ 研判情况：...                              │       │
│  │ ┌────┐ ┌────┐ ┌────┐  ← 点击放大         │       │
│  │ │IMG │ │IMG │ │IMG │                      │       │
│  │ └────┘ └────┘ └────┘                      │       │
│  └──────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────┐       │
│  │ 🔄 闭环与交接                             │       │
│  │ 闭环情况：...  后续移交：...  人员交接：... │       │
│  │ 创建时间：...          更新时间：...        │       │
│  └──────────────────────────────────────────┘       │
│                                                      │
│  [← 返回列表]                      [✏️ 编辑]         │
└─────────────────────────────────────────────────────┘
```

### 6.2 图片查看器

详情页图片缩略图的 `src` 来自后端 `GET /api/ledger/<id>` 返回的 `analysis_images` 数组，每一项包含 `id` 和 `url` 字段（如 `/api/ledger/1/image/a1b2c3d4.jpg`）。

点击缩略图时，使用 Bootstrap Modal 显示全尺寸图片：

```html
<div class="modal fade" x-ref="imageModal" tabindex="-1">
  <div class="modal-dialog modal-xl modal-dialog-centered">
    <div class="modal-content bg-dark">
      <div class="modal-body text-center p-0">
        <img :src="previewImageUrl" class="img-fluid" style="max-height: 80vh">
      </div>
    </div>
  </div>
</div>
```

### 6.3 状态标签颜色规范

| 状态 | 标签文案 | Bootstrap class | 颜色语义 |
|------|---------|-----------------|---------|
| 已办结 | 已办结 | badge bg-success | 绿色 |
| 未办结 | 未办结 | badge bg-warning text-dark | 橙色 |
| 已提级 | 已提级 | badge bg-danger | 红色 |
| 未提级 | 未提级 | badge bg-secondary | 灰色 |

---

## 七、全局 UI 组件

### 7.1 Toast 消息（components.js）

使用 Bootstrap 5 Toast 组件，全局单例：

```javascript
// components.js

const Toast = {
  show(message, type = 'success', duration = 3000) {
    const container = document.getElementById('globalToastContainer');
    const bgClass = {
      success: 'bg-success text-white',
      error:   'bg-danger text-white',
      warning: 'bg-warning text-dark',
      info:    'bg-info text-dark'
    }[type] || 'bg-success text-white';

    const icon = {
      success: 'bi-check-circle-fill',
      error:   'bi-exclamation-triangle-fill',
      warning: 'bi-exclamation-circle-fill',
      info:    'bi-info-circle-fill'
    }[type] || 'bi-check-circle-fill';

    const html = `
      <div class="toast align-items-center ${bgClass} border-0" role="alert">
        <div class="d-flex">
          <div class="toast-body"><i class="bi ${icon} me-2"></i>${message}</div>
          <button class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
        </div>
      </div>
    `;
    const el = document.createElement('div');
    el.innerHTML = html;
    container.appendChild(el.firstElementChild);
    const toastEl = container.lastElementChild;
    const toast = new bootstrap.Toast(toastEl, { delay: duration });
    toast.show();
    toastEl.addEventListener('hidden.bs.toast', () => toastEl.remove());
  }
};
```

### 7.2 确认弹窗（components.js）

```javascript
function confirmModal(title, message, onConfirm) {
  // 动态创建并展示 Bootstrap Modal
  // 确认后执行 onConfirm 回调
}
```

### 7.3 加载状态按钮

提交按钮的统一交互模式：

```html
<button type="button" class="btn btn-primary" :disabled="submitting" @click="submit">
  <span x-show="!submitting">💾 提交保存</span>
  <span x-show="submitting">
    <span class="spinner-border spinner-border-sm me-1"></span>保存中...
  </span>
</button>
```

---

## 八、CSS 自定义样式（style.css）

### 8.1 自定义 Bootstrap 变量覆盖

```css
:root {
  --bs-primary: #1e40af;
  --bs-primary-rgb: 30, 64, 175;
  --bs-success: #16a34a;
  --bs-warning: #f59e0b;
  --bs-danger: #dc2626;
}
```

### 8.2 列表页自定义样式

```css
/* 表格操作列固定在右侧 */
.table-fixed-col-right th:last-child,
.table-fixed-col-right td:last-child {
  position: sticky;
  right: 0;
  background: inherit;
  z-index: 1;
  box-shadow: -2px 0 4px rgba(0,0,0,0.05);
}

/* 基本情况列超长省略 */
.cell-truncate {
  max-width: 180px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  display: inline-block;
}
```

### 8.3 表单页自定义样式

```css
/* 图片上传区域 */
.upload-zone {
  border: 2px dashed var(--bs-border-color);
  border-radius: 8px;
  padding: 2rem;
  text-align: center;
  cursor: pointer;
  transition: border-color 0.2s;
}
.upload-zone:hover, .upload-zone.drag-over {
  border-color: var(--bs-primary);
  background: rgba(var(--bs-primary-rgb), 0.04);
}

/* 图片缩略图网格 */
.image-preview-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
  gap: 0.75rem;
}
.image-preview-item {
  position: relative;
  aspect-ratio: 1;
  border-radius: 6px;
  overflow: hidden;
  border: 1px solid var(--bs-border-color);
}
.image-preview-item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.image-preview-item .btn-remove {
  position: absolute;
  top: 4px;
  right: 4px;
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background: rgba(220,38,38,0.85);
  color: #fff;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  cursor: pointer;
}
```

### 8.4 响应式断点适配

| 断点 | 适配策略 |
|------|---------|
| ≥1200px | 搜索栏一行展开，表格全宽显示 |
| 768px ~ 1199px | 搜索栏部分换行，表格水平滚动 |
| <768px | 搜索栏全换行，操作按钮改为图标，分页按钮精简 |

---

## 九、数据流与状态管理

### 9.1 页面间数据传递

| 场景 | 方案 |
|------|------|
| 列表页 → 详情页 | URL 路径参数：`/ledger/123/view` |
| 列表页 → 编辑页 | URL 路径参数：`/ledger/123/edit` |
| 表单提交成功 → 列表页 | 服务端重定向 + Flash 消息（或前端 `location.href` 跳转） |
| 枚举数据 | 每个页面独立调用 `GET /api/enums`，或存入 `localStorage` 短期缓存（5 分钟 TTL） |

### 9.2 枚举数据缓存策略

```javascript
// utils.js

const enumCache = {
  _data: null,
  _expires: 0,

  async get() {
    if (this._data && Date.now() < this._expires) {
      return this._data;
    }
    const res = await api.get('/api/enums');
    this._data = res.data;
    this._expires = Date.now() + 5 * 60 * 1000; // 5分钟
    return this._data;
  },

  clear() {
    this._data = null;
    this._expires = 0;
  }
};
```

### 9.3 序号自动生成策略

采用**提交时后端自动生成**方案：

- 新增页面加载时，序号框显示 placeholder "留空则自动生成"
- 若用户手动输入，则使用用户值提交
- 若提交时空值，后端在创建记录时自动生成序号，将生成的序号在 API 响应中返回
- 前端接收返回的序号后展示在 Toast 中（例如："台账 JQ-20260715-001 创建成功"）

---

## 十、跨页面行为

### 10.1 错误处理矩阵

| 错误场景 | 前端表现 |
|---------|---------|
| 网络断开 | Toast "网络异常，请检查网络连接"，保留当前页面状态 |
| API 返回 400 | 显示后端返回的 `message`，不关闭当前页面 |
| API 返回 404 | Toast "记录不存在或已被删除"，操作列移除该行 或 跳转列表页 |
| API 返回 500 | Toast "服务器内部错误，请稍后重试" |
| 请求超时（30s） | Toast "请求超时，请稍后重试" |
| 图片上传超限 | 选择文件时前端拦截，Toast "文件大小不能超过 10MB" |
| 图片数量超限 | 选择文件时前端拦截，Toast "最多上传 9 张图片" |

### 10.2 表单未保存离开保护

```javascript
// 在 ledger_form.html 的 Alpine init 中
window.addEventListener('beforeunload', (e) => {
  if (this.dirty && !this.submitting) {
    e.preventDefault();
    e.returnValue = '';
  }
});

// 页面内链接跳转（取消按钮）也需拦截
cancel() {
  if (this.dirty) {
    confirmModal('提示', '表单数据尚未保存，确定要离开吗？', () => {
      window.location.href = '/';
    });
  } else {
    window.location.href = '/';
  }
}
```

### 10.3 序号重复并发处理

如果后端返回 `code: 400, message: "序号 JQ-... 已存在"`，前端应：
1. Toast 显示错误消息
2. 将 serial_no 输入框高亮标红
3. 不关闭页面，允许用户修改后重新提交

---

## 十一、文件清单

```
app/
├── templates/
│   ├── base.html              # 公共布局模板
│   ├── index.html             # 台账列表页
│   ├── ledger_form.html       # 新增/编辑表单页
│   └── ledger_detail.html     # 台账详情页
│
└── static/
    ├── css/
    │   └── style.css          # 自定义样式（覆盖变量 + 组件 + 响应式）
    └── js/
        ├── utils.js           # 工具函数（防抖、日期格式化、文件工具）
        ├── api.js             # Fetch 统一封装
        └── components.js      # 全局 UI 组件（Toast、Confirm、图片预览）
```

---

## 十二、与后端的协议约定（需后端对齐）

以下是前端依赖后端提供的具体行为约定：

| 编号 | 约定内容 | 前端依赖 |
|------|---------|---------|
| 1 | `GET /api/enums` 返回 `{info_source: [{value,label}], case_category: [{value,label}]}`，key 名与数据库字段名一致 | 所有下拉框 |
| 2 | `GET /api/ledger` 列表接口返回的 `analysis_images` 为 `null` 或空数组（不返回完整图片数据，减少传输量） | 列表页 |
| 3 | `GET /api/ledger/<id>` 详情接口返回的 `analysis_images` 为 `[{id, url}]` 对象数组 | 详情页缩略图、编辑页已有图片回显 |
| 4 | 编辑文本字段走 `PATCH /api/ledger/<id>`（纯 JSON），`Content-Type: application/json` | 编辑表单提交 |
| 5 | 提交表单时，`is_closed` 和 `is_escalated` 为布尔值 `true` / `false`（JSON body 中自然传递） | 表单提交 |
| 6 | 提交表单时，序号为空字符串表示"请后端自动生成" | 新增表单 |
| 7 | 后端需启用 CSRF 保护，前端所有 Fetch 请求自动附带 `X-CSRFToken` header | 全部 API 调用 |
| 8 | 图片上传走独立接口 `POST /api/ledger/<id>/images`，字段名 `file`，由后端 UUID 重命名 | 图片上传 |
| 9 | 图片删除走独立接口 `DELETE /api/ledger/<id>/images/<image_id>` | 编辑表单图片管理 |
| 10 | 列表接口支持 `sort_by` / `sort_order` 排序参数 | 列表页排序 |
