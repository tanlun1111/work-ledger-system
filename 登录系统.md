# 警情案件工作台账登记系统 — 登录系统设计

## 一、设计原则

| 原则 | 说明 |
|------|------|
| **无注册功能** | 不提供前端注册入口，所有账号由管理员通过 CLI 脚本或数据库直接导入 |
| **Session 认证** | 使用 Flask 内置 session（服务端 cookie），与 Jinja2 模板渲染天然适配 |
| **密码哈希** | 使用现有依赖 `Werkzeug` 内置的 `generate_password_hash` / `check_password_hash` |
| **路由拦截** | 通过装饰器 `@login_required` 保护页面路由和 API 路由 |

---

## 二、数据库设计

### 2.1 新增数据表：sys_user（系统用户表）

| 序号 | 字段名 | 中文名称 | 类型 | 约束 | 说明 |
|------|--------|---------|------|------|------|
| 1 | id | 主键ID | INTEGER | PK, AUTO_INCREMENT | — |
| 2 | username | 用户名 | VARCHAR(50) | NOT NULL, UNIQUE | 登录用户名 |
| 3 | password_hash | 密码哈希 | VARCHAR(255) | NOT NULL | Werkzeug 生成的哈希值 |
| 4 | display_name | 显示名称 | VARCHAR(50) | NOT NULL | 页面展示的姓名 |
| 5 | role | 角色 | VARCHAR(20) | NOT NULL, DEFAULT 'user' | `admin` 或 `user` |
| 6 | is_active | 是否启用 | INTEGER | NOT NULL, DEFAULT 1 | 0=禁用, 1=启用 |
| 7 | last_login_at | 最后登录时间 | DATETIME | — | 记录上次登录时间 |
| 8 | created_at | 创建时间 | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | — |
| 9 | updated_at | 更新时间 | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | — |

```sql
CREATE TABLE sys_user (
    id              INTEGER PRIMARY KEY AUTO_INCREMENT,
    username        VARCHAR(50)  NOT NULL UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    display_name    VARCHAR(50)  NOT NULL,
    role            VARCHAR(20)  NOT NULL DEFAULT 'user',
    is_active       INTEGER      NOT NULL DEFAULT 1,
    last_login_at   DATETIME,
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2.2 角色说明

| 角色 | 值 | 权限 |
|------|-----|------|
| 管理员 | `admin` | 查看/新增/编辑/删除所有台账、导入用户、管理枚举配置 |
| 普通用户 | `user` | 查看/新增/编辑自己的台账、无法删除 |

### 2.3 默认管理员账号

系统首次启动时自动创建默认管理员：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| `admin` | `admin123` | admin |

> 启动后应立即修改默认密码。

### 2.4 ORM 模型

```python
# app/models/user.py

from datetime import datetime
from app import db
from werkzeug.security import generate_password_hash, check_password_hash


class User(db.Model):
    __tablename__ = "sys_user"

    id = db.Column(db.Integer, primary_key=True, autoincrement=True)
    username = db.Column(db.String(50), nullable=False, unique=True, index=True)
    password_hash = db.Column(db.String(255), nullable=False)
    display_name = db.Column(db.String(50), nullable=False)
    role = db.Column(db.String(20), nullable=False, default="user")
    is_active = db.Column(db.Integer, nullable=False, default=1)
    last_login_at = db.Column(db.DateTime)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.now)
    updated_at = db.Column(db.DateTime, nullable=False, default=datetime.now, onupdate=datetime.now)

    @classmethod
    def init_default_admin(cls):
        if not cls.query.filter_by(username="admin").first():
            admin = cls(
                username="admin",
                password_hash=generate_password_hash("admin123"),
                display_name="管理员",
                role="admin",
                is_active=1,
            )
            db.session.add(admin)
            db.session.commit()

    def set_password(self, password):
        self.password_hash = generate_password_hash(password)

    def check_password(self, password):
        return check_password_hash(self.password_hash, password)

    def is_admin(self):
        return self.role == "admin"
```

---

## 三、认证流程

### 3.1 登录流程

```
用户访问任何页面
    → @login_required 拦截器检查 session
    → 未登录 → 重定向到 GET /login
    → 用户输入用户名 + 密码 → POST /login
    → 后端校验 → 成功 → session["user_id"] = user.id
    → 重定向到 GET /（台账列表页）
```

### 3.2 登出流程

```
用户点击"退出登录"
    → GET /logout
    → session.clear()
    → 重定向到 GET /login
```

### 3.3 密码修改流程

```
用户点击"修改密码"
    → GET /change-password（模态框或独立页）
    → 输入 旧密码 + 新密码 + 确认新密码
    → POST /api/change-password
    → 校验旧密码 → 更新密码哈希 → 返回成功
```

### 3.4 密码策略

| 约束 | 值 |
|------|-----|
| 最小长度 | 6 字符 |
| 复杂度 | 无强制要求（内网系统） |
| 哈希算法 | Werkzeug 默认（pbkdf2:sha256） |

---

## 四、认证中间件

### 4.1 `@login_required` 装饰器

```python
# app/utils/auth.py

from functools import wraps
from flask import session, redirect, url_for, request, jsonify, g
from app.models.user import User


def login_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        user_id = session.get("user_id")
        if not user_id:
            if request.path.startswith("/api/"):
                return jsonify({"code": 401, "message": "请先登录", "data": {}}), 401
            return redirect(url_for("auth.login"))
        user = User.query.get(user_id)
        if not user or not user.is_active:
            session.clear()
            if request.path.startswith("/api/"):
                return jsonify({"code": 401, "message": "账号已被禁用", "data": {}}), 401
            return redirect(url_for("auth.login"))
        g.current_user = user
        return f(user, *args, **kwargs)
    return decorated
```

使用示例：

```python
@main_bp.route("/")
@login_required
def index(user):
    return render_template("index.html")
```

### 4.2 `@admin_required` 装饰器

可独立使用，不依赖 `@login_required` 注入 user：

```python
def admin_required(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        user_id = session.get("user_id")
        if not user_id:
            if request.path.startswith("/api/"):
                return jsonify({"code": 401, "message": "请先登录", "data": {}}), 401
            return redirect(url_for("auth.login"))
        user = User.query.get(user_id)
        if not user or not user.is_active:
            session.clear()
            if request.path.startswith("/api/"):
                return jsonify({"code": 401, "message": "账号已被禁用", "data": {}}), 401
            return redirect(url_for("auth.login"))
        if not user.is_admin():
            if request.path.startswith("/api/"):
                return jsonify({"code": 403, "message": "需要管理员权限", "data": {}}), 403
            return redirect(url_for("main.index"))
        return f(user, *args, **kwargs)
    return decorated
```

### 4.3 模板全局注入当前用户

在工厂函数中注册 `@app.context_processor`，使所有模板可以直接访问 `{{ current_user }}`：

```python
# app/__init__.py 中新增
@app.context_processor
def inject_current_user():
    from app.models.user import User
    from flask import g

    if hasattr(g, 'current_user'):
        return {"current_user": g.current_user}

    uid = session.get("user_id")
    if uid:
        user = User.query.get(uid)
        if user and user.is_active:
            g.current_user = user
            return {"current_user": user}
    return {}
```

模板中使用：

```jinja2
<!-- base.html 导航栏 -->
<span>{{ current_user.display_name }}</span>
<a href="{{ url_for('auth.logout') }}">退出登录</a>
```

---

## 五、认证路由设计

### 5.1 新增蓝图：auth

```python
# app/routes/auth.py

from flask import Blueprint, render_template, request, redirect, url_for, session, jsonify
from app.models.user import User
from app.utils.response import success

auth_bp = Blueprint("auth", __name__)
```

### 5.2 API 接口

#### 5.2.1 登录页面

```
GET /login
```

返回 `login.html`（独立页面，不继承 base.html）。

#### 5.2.2 提交登录

```
POST /login
Content-Type: application/x-www-form-urlencoded
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

**成功响应**：302 重定向到 `/`

**失败响应**：302 重定向回 `/login?error=用户名或密码错误`

#### 5.2.3 登出

```
GET /logout
```

清除 session，302 重定向到 `/login`。

#### 5.2.4 修改密码

```
POST /api/change-password
Content-Type: application/json
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| old_password | string | 是 | 旧密码 |
| new_password | string | 是 | 新密码（最少 6 位） |

**响应**：

```json
{
  "code": 200,
  "message": "密码修改成功",
  "data": {}
}
```

#### 5.2.5 获取当前用户信息

```
GET /api/me
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "username": "admin",
    "display_name": "管理员",
    "role": "admin"
  }
}
```

### 5.3 路由实现

```python
# app/routes/auth.py

from flask import Blueprint, render_template, request, redirect, url_for, session
from datetime import datetime
from app.models.user import User

auth_bp = Blueprint("auth", __name__)


@auth_bp.route("/login", methods=["GET", "POST"])
def login():
    if request.method == "GET":
        if session.get("user_id"):
            return redirect(url_for("main.index"))
        error = request.args.get("error", "")
        expired = request.args.get("expired", "")
        return render_template("login.html", error=error, expired=expired)

    username = request.form.get("username", "").strip()
    password = request.form.get("password", "")

    if not username or not password:
        return redirect(url_for("auth.login", error="请输入用户名和密码"))

    user = User.query.filter_by(username=username).first()
    if not user or not user.is_active or not user.check_password(password):
        return redirect(url_for("auth.login", error="用户名或密码错误"))

    session["user_id"] = user.id
    session.permanent = True
    user.last_login_at = datetime.now()
    from app import db
    db.session.commit()

    return redirect(url_for("main.index"))


@auth_bp.route("/logout")
def logout():
    session.clear()
    return redirect(url_for("auth.login"))
```

> **改密接口** `POST /api/change-password` 和 `GET /api/me` 放在 `api_bp`（`app/routes/api.py`）中，
> 与现有 API 路由统一管理，并在路由函数上添加 `@login_required`：

```python
# app/routes/api.py 中新增

from flask import jsonify

@api_bp.route("/change-password", methods=["POST"])
@login_required
def change_password(user):
    data = request.get_json(silent=True) or {}
    old_pw = data.get("old_password", "")
    new_pw = data.get("new_password", "")

    if not old_pw or not new_pw:
        return jsonify({"code": 400, "message": "请填写旧密码和新密码", "data": {}}), 400

    if len(new_pw) < 6:
        return jsonify({"code": 400, "message": "新密码最少 6 位", "data": {}}), 400

    if not user.check_password(old_pw):
        return jsonify({"code": 400, "message": "旧密码错误", "data": {}}), 400

    user.set_password(new_pw)
    from app import db
    db.session.commit()

    return success(message="密码修改成功")


@api_bp.route("/me")
@login_required
def get_me(user):
    return success({
        "id": user.id,
        "username": user.username,
        "display_name": user.display_name,
        "role": user.role,
    })
```

---

## 六、用户管理 CLI 脚本

### 6.1 脚本位置

```
backend/
└── manage.py                    # 用户管理 CLI 脚本
```

### 6.2 脚本功能

```python
# manage.py

import sys
import os
sys.path.insert(0, os.path.dirname(__file__))

from flask import Flask
from config import config_map
from app import db
from app.models.user import User
from werkzeug.security import generate_password_hash

app = Flask(__name__)
app.config.from_object(config_map["development"])
db.init_app(app)


def usage():
    print("""
用户管理脚本

用法:
  python manage.py create-user <username> <password> <display_name> [admin|user]
  python manage.py reset-password <username> <new_password>
  python manage.py list-users
  python manage.py disable-user <username>
  python manage.py enable-user <username>
""")


def create_user():
    if len(sys.argv) < 5:
        print("用法: python manage.py create-user <username> <password> <display_name> [admin|user]")
        return
    username = sys.argv[2]
    password = sys.argv[3]
    display_name = sys.argv[4]
    role = sys.argv[5] if len(sys.argv) > 5 else "user"

    with app.app_context():
        if User.query.filter_by(username=username).first():
            print(f"用户 {username} 已存在")
            return
        user = User(
            username=username,
            password_hash=generate_password_hash(password),
            display_name=display_name,
            role=role,
        )
        db.session.add(user)
        db.session.commit()
        print(f"用户 {username}（{display_name}，角色 {role}）创建成功")


def reset_password():
    if len(sys.argv) < 4:
        print("用法: python manage.py reset-password <username> <new_password>")
        return
    username = sys.argv[2]
    new_password = sys.argv[3]

    with app.app_context():
        user = User.query.filter_by(username=username).first()
        if not user:
            print(f"用户 {username} 不存在")
            return
        user.set_password(new_password)
        db.session.commit()
        print(f"用户 {username} 密码已重置")


def list_users():
    with app.app_context():
        users = User.query.all()
        print(f"{'ID':<5} {'用户名':<20} {'显示名称':<15} {'角色':<8} {'状态':<6} {'最后登录'}")
        print("-" * 80)
        for u in users:
            status = "启用" if u.is_active else "禁用"
            last = u.last_login_at.strftime("%Y-%m-%d %H:%M") if u.last_login_at else "-"
            print(f"{u.id:<5} {u.username:<20} {u.display_name:<15} {u.role:<8} {status:<6} {last}")


def disable_user():
    if len(sys.argv) < 3:
        print("用法: python manage.py disable-user <username>")
        return
    _set_user_active(sys.argv[2], 0)


def enable_user():
    if len(sys.argv) < 3:
        print("用法: python manage.py enable-user <username>")
        return
    _set_user_active(sys.argv[2], 1)


def _set_user_active(username, active):
    with app.app_context():
        user = User.query.filter_by(username=username).first()
        if not user:
            print(f"用户 {username} 不存在")
            return
        user.is_active = active
        db.session.commit()
        status = "启用" if active else "禁用"
        print(f"用户 {username} 已{status}")


if __name__ == "__main__":
    cmds = {
        "create-user": create_user,
        "reset-password": reset_password,
        "list-users": list_users,
        "disable-user": disable_user,
        "enable-user": enable_user,
    }
    if len(sys.argv) < 2 or sys.argv[1] not in cmds:
        usage()
    else:
        cmds[sys.argv[1]]()
```

### 6.3 使用示例

```bash
# 创建用户
python manage.py create-user zhangsan pass1234 张三 user
python manage.py create-user lisi pass1234 李四 admin

# 查看所有用户
python manage.py list-users

# 重置密码
python manage.py reset-password zhangsan newpass5678

# 禁用/启用用户
python manage.py disable-user zhangsan
python manage.py enable-user zhangsan
```

---

## 七、现有路由改造

### 7.1 主路由（main.py）添加 `@login_required`

```python
from flask import Blueprint, render_template
from app.utils.auth import login_required

main_bp = Blueprint("main", __name__)


@main_bp.route("/")
@login_required
def index(user):
    return render_template("index.html")


@main_bp.route("/ledger/<int:ledger_id>/view")
@login_required
def ledger_view(user, ledger_id):
    from app.services.ledger_service import get_ledger_detail
    from app.services.image_service import get_ledger_images
    record = get_ledger_detail(ledger_id)
    images = get_ledger_images(ledger_id)
    return render_template("ledger_detail.html", record=record, images=images)
```

> **注意**: 前端已改为弹窗模式，`/ledger/add` 和 `/ledger/<id>/edit` 路由不再需要页面跳转，
> 可删除或改为重定向到 `/`。新增/编辑均由列表页和详情页中的 `openLedgerForm()` 函数触发 Bootstrap Modal。


### 7.2 API 路由（api.py）添加 `@login_required`

```python
from app.utils.auth import login_required

@api_bp.route("/ledger", methods=["GET"])
@login_required
def list_ledgers(user):
    # ... 原有逻辑不变 ...

@api_bp.route("/ledger", methods=["POST"])
@login_required
def create_ledger(user):
    # ... 原有逻辑不变 ...

# 其余 API 接口同理添加 @login_required
```

### 7.2.1 前端 api.js 全局 401 拦截

当 session 过期后，API 返回 `{"code": 401}`，前端需要自动跳转到登录页。
在 `api.js` 的 `request` 方法中增加 401 检测：

```javascript
// api.js — request() 方法中，json.code 判断之前插入

var json = await res.json();
// 新增：401 全局拦截
if (json.code === 401) {
    window.location.href = '/login?expired=1';
    throw { code: 401, message: json.message };
}
if (json.code !== 200) {
    throw { code: json.code, message: json.message };
}
```

> 插入位置在 `var json = await res.json();` 之后、`if (json.code !== 200)` 之前。
> 这样 session 过期后用户的操作不会被静默忽略，能看到"登录已过期"的提示。

### 7.3 工厂函数注册 auth 蓝图

在 `app/__init__.py` 的 `create_app` 中增加：

```python
from app.routes.auth import auth_bp
app.register_blueprint(auth_bp)

# 注册模板全局变量
@app.context_processor
def inject_current_user():
    from app.models.user import User
    from flask import g

    if hasattr(g, 'current_user'):
        return {"current_user": g.current_user}

    uid = session.get("user_id")
    if uid:
        user = User.query.get(uid)
        if user and user.is_active:
            g.current_user = user
            return {"current_user": user}
    return {}
```

并在 `db.create_all()` 之后初始化默认管理员：

```python
from app.models.user import User
User.init_default_admin()
```

---

## 八、前端页面设计

### 8.1 登录页（templates/login.html）

独立页面，不继承 `base.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="{{ csrf_token() }}">
  <title>登录 - 警情案件工作台账登记系统</title>
  <link href="https://cdn.bootcdn.net/ajax/libs/twitter-bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.bootcdn.net/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    body { background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%); min-height: 100vh; }
    .login-card { max-width: 400px; margin: 10vh auto 0; }
  </style>
</head>
<body>
  <div class="login-card">
    <div class="card shadow-lg border-0">
      <div class="card-body p-5">
        <div class="text-center mb-4">
          <i class="bi bi-shield-check fs-1 text-primary"></i>
          <h4 class="fw-bold mt-2">警情案件工作台账登记系统</h4>
        </div>

        {% if expired %}
        <div class="alert alert-warning py-2 small">登录已过期，请重新登录</div>
        {% endif %}

        {% if error %}
        <div class="alert alert-danger py-2 small">{{ error }}</div>
        {% endif %}

        <form method="POST" action="{{ url_for('auth.login') }}">
          <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
          <div class="mb-3">
            <label class="form-label small">用户名</label>
            <input type="text" name="username" class="form-control" required autofocus>
          </div>
          <div class="mb-3">
            <label class="form-label small">密码</label>
            <input type="password" name="password" class="form-control" required>
          </div>
          <button type="submit" class="btn btn-primary w-100">登 录</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
```

### 8.2 导航栏修改（base.html）

将现有的静态导航栏用户区：

```html
<div class="d-flex align-items-center ms-auto gap-2">
  <span class="text-white-50" style="font-size:13px">
    <i class="bi bi-person-circle me-1"></i>管理员
  </span>
</div>
```

替换为动态用户信息 + 修改密码 + 退出登录：

```jinja2
<div class="d-flex align-items-center ms-auto gap-2">
  <span class="text-white-50" style="font-size:13px">
    <i class="bi bi-person-circle me-1"></i>{{ current_user.display_name }}
    <span class="badge bg-light text-dark ms-1" style="font-size:11px">
      {{ '管理员' if current_user.is_admin() else '用户' }}
    </span>
  </span>
  <button class="btn btn-outline-light btn-sm" data-bs-toggle="modal" data-bs-target="#changePasswordModal">
    <i class="bi bi-key me-1"></i>修改密码
  </button>
  <a href="{{ url_for('auth.logout') }}" class="btn btn-outline-light btn-sm">
    <i class="bi bi-box-arrow-right me-1"></i>退出
  </a>
</div>
```

### 8.3 修改密码弹窗（base.html 尾部）

在 `base.html` 的 `</body>` 前插入一个 Bootstrap Modal：

```html
<div class="modal fade" id="changePasswordModal" tabindex="-1">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">修改密码</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <div class="mb-3">
          <label class="form-label small">旧密码</label>
          <input type="password" class="form-control form-control-sm" id="oldPw">
        </div>
        <div class="mb-3">
          <label class="form-label small">新密码（最少 6 位）</label>
          <input type="password" class="form-control form-control-sm" id="newPw">
        </div>
        <div class="mb-0">
          <label class="form-label small">确认新密码</label>
          <input type="password" class="form-control form-control-sm" id="confirmPw">
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">取消</button>
        <button type="button" class="btn btn-primary btn-sm" onclick="changePassword()">确认修改</button>
      </div>
    </div>
  </div>
</div>
```

`components.js` 中新增 `changePassword` 函数：

```javascript
async function changePassword() {
  const oldPw = document.getElementById('oldPw').value;
  const newPw = document.getElementById('newPw').value;
  const confirmPw = document.getElementById('confirmPw').value;

  if (!oldPw || !newPw) { Toast.show('请填写旧密码和新密码', 'warning'); return; }
  if (newPw.length < 6) { Toast.show('新密码最少 6 位', 'warning'); return; }
  if (newPw !== confirmPw) { Toast.show('两次输入的新密码不一致', 'warning'); return; }

  const btn = document.querySelector('#changePasswordModal .btn-primary');
  btn.disabled = true;
  btn.innerHTML = '<span class="spinner-border spinner-border-sm me-1"></span>修改中...';

  try {
    await api.post('/api/change-password', { old_password: oldPw, new_password: newPw });
    Toast.show('密码修改成功');
    document.getElementById('oldPw').value = '';
    document.getElementById('newPw').value = '';
    document.getElementById('confirmPw').value = '';
    bootstrap.Modal.getInstance(document.getElementById('changePasswordModal')).hide();
  } catch (e) {
    Toast.show(e.message || '修改失败', 'error');
  } finally {
    btn.disabled = false;
    btn.innerHTML = '确认修改';
  }
}
```

---

## 九、文件清单

```
backend/
├── manage.py                           # 新增 - 用户管理 CLI 脚本
├── config.py                           # 修改 - 需增加 SESSION 相关配置 + timedelta 导入
├── app/
│   ├── __init__.py                     # 修改 - 注册 auth_bp + context_processor + init_default_admin
│   ├── models/
│   │   └── user.py                     # 新增 - User ORM 模型
│   ├── routes/
│   │   ├── auth.py                     # 新增 - 登录/登出路由
│   │   ├── main.py                     # 修改 - 所有路由加 @login_required，ledger_view 传完整对象
│   │   └── api.py                      # 修改 - 所有 API 加 @login_required + change_password/me 端点
│   ├── utils/
│   │   └── auth.py                     # 新增 - login_required / admin_required 装饰器
│   ├── templates/
│   │   ├── login.html                  # 新增 - 登录页面
│   │   └── base.html                   # 修改 - 导航栏展示用户信息 + 修改密码弹窗
│   └── static/
│       └── js/
│           ├── api.js                  # 修改 - 增加 401 全局拦截跳转
│           └── components.js           # 修改 - 新增 changePassword 函数
```

---

## 十、配置文件修改

`config.py` 中 `Config` 基类需要增加 session 相关配置：

```python
class Config:
    # ... 原有配置 ...

    # Session 配置
    SESSION_COOKIE_HTTPONLY = True
    SESSION_COOKIE_SAMESITE = "Lax"
    PERMANENT_SESSION_LIFETIME = timedelta(hours=24)   # 24 小时无操作自动过期
```

> `timedelta` 需从 `datetime` 导入：`from datetime import timedelta`

---

## 十一、认证状态流转图

```
                  ┌─────────────┐
                  │  访问任何页面  │
                  └──────┬──────┘
                         │
                    @login_required
                         │
              ┌──────────▼──────────┐
              │   session 有 user_id? │
              └──────┬──────┬──────┘
                     │YES   │NO
                     ▼      ▼
              ┌─────────┐  ┌──────────┐
              │ 正常访问  │  │ 重定向到   │
              │ 页面/A PI │  │ /login    │
              └─────────┘  └─────┬────┘
                                 │
                          POST /login
                                 │
                       ┌─────────▼─────────┐
                       │ 校验用户名 + 密码    │
                       └────┬──────┬───────┘
                            │OK    │FAIL
                            ▼      ▼
                     ┌──────────┐ ┌──────────────┐
                     │ 设置      │ │ /login?error= │
                     │ session   │ │               │
                     │ 跳转首页   │ └──────────────┘
                     └──────────┘
```
