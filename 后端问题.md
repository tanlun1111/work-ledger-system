# 后端设计文档 — 问题清单

> 基于后端设计.md与前端设计.md、总设计文档的交叉审查结果

---

## 问题一：缺失 `/api/ledger/next-serial` 接口【高】

**位置**：[前端设计.md - 5.3](file:///d:/408/前端设计.md#L467)

前端调用了 `GET /api/ledger/next-serial` 来获取自动生成的序号，但后端设计中**没有定义这个接口**。

**方案一（推荐）**：在后端 API 设计中新增：

```
GET /api/ledger/next-serial
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "serial_no": "JQ-20260715-0001"
  }
}
```

**方案二**：不在新建台账前单独获取序号，改为：
- 前端不填 `serial_no`
- 后端在 `POST /api/ledger` 内部自动生成序号
- 响应中直接返回生成的序号

---

## 问题二：枚举接口返回格式与前端期望不一致【高】

**位置**：[后端设计.md - 4.4.1](file:///d:/408/后端设计.md#L449) 和 [前端设计.md - 3.3](file:///d:/408/前端设计.md#L221)

**后端当前返回**：
```json
{
  "data": {
    "info_source": ["110接报", "群众报案", "上级交办"],
    "case_category": ["刑事", "治安", "交通"]
  }
}
```

**前端期望格式**：
```json
{
  "data": {
    "info_sources": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"}
    ],
    "case_categories": [...]
  }
}
```

差异：
1. **key 名单复数不一致**：后端单数 `info_source`，前端复数 `info_sources`
2. **value 结构不一致**：后端字符串数组，前端 `[{value, label}]` 对象数组

**建议修改**：将后端枚举接口返回改为对象数组格式，key 名与前端协定统一。对象数组可扩展（未来加 disabled、color 等属性）：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "info_source": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"},
      {"value": "上级交办", "label": "上级交办"},
      {"value": "其他部门移交", "label": "其他部门移交"},
      {"value": "主动发现", "label": "主动发现"},
      {"value": "其他", "label": "其他"}
    ],
    "case_category": [
      {"value": "刑事", "label": "刑事"},
      {"value": "治安", "label": "治安"},
      {"value": "交通", "label": "交通"},
      {"value": "求助", "label": "求助"},
      {"value": "纠纷", "label": "纠纷"},
      {"value": "其他", "label": "其他"}
    ]
  }
}
```

---

## 问题三：编辑接口 HTTP 方法与前端不对齐【高】

**位置**：[后端设计.md - 4.2.2](file:///d:/408/后端设计.md#L253)

**现状**：
- 后端编辑接口使用 `PATCH /api/ledger/<id>`
- 前端 `api.js` 中只有 `put` 方法，提交编辑时发送 `PUT`

**影响**：前端 PUT 请求可能匹配不到后端路由（取决于后端路由注册方式），需要前后端统一。

**建议**：**以 PATCH 为准**（语义更正确），通知前端将 `api.put` 改为 `api.patch`，或在 `api.js` 中新增 `patch` 方法。

---

## 问题四：数据库设计（图片存储方式）与总设计冲突【高】

**位置**：[后端设计.md - 2.3](file:///d:/408/后端设计.md#L67) 和 [总设计文档 - 2.1](file:///d:/408/工作台账登记系统-设计文档.md#L51)

**后端**：独立 `ledger_images` 表，`work_ledger` 表**不包含** `analysis_images` 字段

**总设计**：`work_ledger` 表**包含** `analysis_images` TEXT 字段（逗号分隔路径）

**说明**：后端方案更优（支持 UUID 安全重命名、原始文件名记录、MIME 校验、排序、独立查询），但总设计文档未同步。**后端设计本身没有问题**，问题在于总设计文档需要更新以匹配后端实际实现。

**修复**：更新总设计文档的数据库设计章节，删除 `analysis_images` 字段，新增 `ledger_images` 独立表说明。

---

## 问题五：枚举配置表缺少必要字段【中】

**位置**：[后端设计.md - 2.4](file:///d:/408/后端设计.md#L82)

`enum_config` 表的设计中，`enum_type` 和 `enum_value` 直接存显示文本，缺少一个"内部编码"字段。

考虑场景：前端提交 `info_source: "110接报"`，后端校验时用字符串比对，一旦管理员把 `"110接报"` 改为 `"110报警"`，历史数据中的 `"110接报"` 就匹配不上了。

**建议**：拆分 `code` 和 `label`：
```sql
enum_type   VARCHAR(50),  -- 如 'info_source'
enum_code   VARCHAR(50),  -- 如 '110_report'（内部编码，永不修改）
enum_label  VARCHAR(100), -- 如 '110接报'（显示文本，可修改）
sort_order  INTEGER,
is_active   INTEGER
```

前端请求参数提交 `code`，后端校验用 `enum_code` 匹配，前端展示用 `enum_label`。

---

## 问题六：列表接口缺少"获取辖区列表"的支持【中】

**位置**：[后端设计.md - 4.2.5](file:///d:/408/后端设计.md#L344)

前端列表页的辖区筛选需要下拉选项，当前设计中：
- `GET /api/enums` 返回 `info_source` 和 `case_category`
- **没有**返回辖区列表

辖区是自由文本字段（VARCHAR），不是枚举表管理的，但前端需要列出现有的辖区值用于 `datalist` 建议。可以：
1. 在 `/api/enums` 中新增 `jurisdictions` 字段，通过 `SELECT DISTINCT jurisdiction FROM work_ledger` 动态统计
2. 或将辖区也纳入 `enum_config` 管理

---

## 问题七：图片上传接口缺少前端所需的"新增时无 ID"场景【中】

**位置**：[后端设计.md - 4.3.1](file:///d:/408/后端设计.md#L393)

`POST /api/ledger/<id>/images` 要求先有 `ledger_id`。新增台账时 `id` 还不存在。

**两种解决思路**：
1. 前端先创建台账（返回 id）→ 再上传图片
2. 提供一个不绑定 ledger 的图片上传接口（如 `POST /api/images`），返回临时文件 ID，创建台账时关联

**建议用方案 1**，但需要在后端设计中明确说明新增台账的交互流程。

---

## 问题八：软删除设计中缺少详情接口的 404 处理说明【中】

**位置**：[后端设计.md - 5.3](file:///d:/408/后端设计.md#L523)

5.3 节提到"详情接口额外处理：若 `is_deleted=1` 返回 404"，这是好的设计，但 4.2.4 的详情接口描述中没有体现这一点。**建议在 4.2.4 API 说明中补充**：已软删除的记录返回 404。

---

## 问题九：数据库索引设计理由应同步到总设计【低】

**位置**：[后端设计.md - 2.5](file:///d:/408/后端设计.md#L101)

后端删除了 `idx_is_closed` 和 `idx_is_escalated`，理由是二值字段选择性低，索引无效，并替换为 `idx_closed_time` 组合索引。这个分析正确，但总设计文档仍保留了这两个无效索引。

**修复**：同步更新总设计文档的索引章节。

---

## 问题十：requirements.txt 中 WTF 依赖缺失【低】

**位置**：[后端设计.md - 6.2](file:///d:/408/后端设计.md#L595)

依赖清单缺少 `Flask-WTF`。虽然代码中用了 `flask_wtf.csrf` 和 `csrf_token()`，但 `Flask-WTF` 在 pip 安装时可能不会自动一起安装。应明确列出：

```
Flask-WTF==1.2.1
```

---

## 总结

| 优先级 | 数量 | 说明 |
|:---:|:---:|------|
| 🔴 高 | 4 个 | 缺失 next-serial 接口、枚举格式不一致、HTTP 方法不对齐、图片存储方式与总设计冲突 |
| 🟡 中 | 4 个 | 枚举表缺 code 字段、辖区列表缺失、新增时图片上传场景、软删除 404 未补充 |
| 🟢 低 | 2 个 | 索引同步、依赖缺失 |
