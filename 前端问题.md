# 前端设计文档 — 问题清单

> 基于前端设计.md与后端设计.md、总设计文档的交叉审查结果

---

## 问题一：API 请求方法与后端不一致【高】

**位置**：[前端设计.md - 3.1 api.js 封装](file:///d:/408/前端设计.md#L119) 和 [后端设计.md - 4.2.2](file:///d:/408/后端设计.md#L256)

**现状**：
- 前端封装了 `api.put(url, data)`，发送 HTTP `PUT` 请求
- 后端编辑接口使用 HTTP `PATCH`

**影响**：前端 `PUT` 请求打到后端后可能被拒绝或走不到正确的路由。`PUT` 语义是完整替换，`PATCH` 是部分更新。后端设计用 PATCH 是正确的（编辑时只提交修改的字段）。

**修复**：将前端 `api.put()` 改为 `api.patch()`，新增 `patch` 方法实现。

---

## 问题二：前端引用了后端不存在的接口【高】

**位置**：[前端设计.md - 5.3 generateSerialNo()](file:///d:/408/前端设计.md#L467)

```javascript
generateSerialNo() { /* 调用 api.get('/api/ledger/next-serial') 获取自动序号 */ }
```

**现状**：前端调用了 `GET /api/ledger/next-serial` 来获取自动生成的序号，但后端设计中**没有定义这个接口**。

**影响**：前端功能直接不可用，新增台账时无法获取自动序号。

**修复**：两种情况：
- 若后端计划提供此接口 → 在后端设计中补充 `GET /api/ledger/next-serial` 的定义
- 若后端改为在新建接口响应中直接返回序号 → 前端改为调用新建接口获取

---

## 问题三：图片上传交互策略与后端不匹配【高】

**位置**：[前端设计.md - 3.2 图片上传的统一策略](file:///d:/408/前端设计.md#L196) 和 [后端设计.md - 4.3.1](file:///d:/408/后端设计.md#L393)

**现状**：
- **前端**：图片增删"不实时提交到服务端"，收集变更后在表单最终提交时一并处理。提交时构建 FormData，包含 `removed_images` (字符串) + `analysis_images` (File[])
- **后端**：图片走**独立接口** `POST /api/ledger/<id>/images`，新增台账接口是纯 JSON（不含文件），图片需先上传再提交台账

**影响**：两种策略完全不同，对接会直接失败：
- 按前端策略 → 后端 JSON 接口收不到文件
- 按后端策略 → 前端未实现独立图片上传和删除接口调用

**修复**：**建议统一为后端策略**（独立图片接口更清晰），前端需调整：
1. 新增页：先新建台账获取 id → 上传图片 → 关联完成
2. 编辑页：已有图片通过详情接口渲染，新增图片走 `POST /api/ledger/<id>/images`，删除走 `DELETE /api/ledger/<id>/images/<image_id>`
3. 移除 "收集变更、最后一起提交" 的 FormData 混合方案

---

## 问题四：字段校验长度与后端不一致【高】

**位置**：[前端设计.md - 5.3 字段校验规则](file:///d:/408/前端设计.md#L480)

| 字段 | 前端校验 | 后端约束 | 冲突 |
|------|----------|----------|:---:|
| `serial_no` | 1-20 字符 | 最长 30 字符 | ✅ 无冲突（前端更严） |
| `duty_officer` | **1-20 字符** | 1-50 字符 | ❌ 后端允许 21~50 字符会被前端拦截 |
| `jurisdiction` | **1-50 字符** | 1-100 字符 | ❌ 后端允许 51~100 字符会被前端拦截 |

**影响**：后端接受的数据在前端校验阶段就被拒绝，用户体验差。

**修复**：统一前端校验规则与后端一致：
- `duty_officer` → `1-50 字符`
- `jurisdiction` → `1-100 字符`

---

## 问题五：枚举接口返回格式与后端不一致【高】

**位置**：[前端设计.md - 3.3 枚举接口约定](file:///d:/408/前端设计.md#L221)

**前端期望格式**：
```json
{
  "data": {
    "info_sources": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"}
    ],
    "case_categories": [...]
  }
}
```

**后端实际返回**（后端设计 4.4.1）：
```json
{
  "data": {
    "info_source": ["110接报", "群众报案", "上级交办", ...],
    "case_category": ["刑事", "治安", "交通", ...]
  }
}
```

两处不一致：
1. **key 名不同**：前端用复数 `info_sources` / `case_categories`，后端用单数 `info_source` / `case_category`
2. **value 结构不同**：前端期望 `[{value, label}]` 对象数组，后端返回纯字符串数组

**影响**：前端下拉框无法渲染。

**修复**：任选一方对齐，**建议后端改为前端格式**（对象数组方便未来扩展，如加 disabled 属性），后端返回：
```json
{
  "data": {
    "info_sources": [
      {"value": "110接报", "label": "110接报"},
      {"value": "群众报案", "label": "群众报案"}
    ],
    "case_categories": [
      {"value": "刑事", "label": "刑事"},
      {"value": "治安", "label": "治安"}
    ]
  }
```
前端同步修改 key 名为后端统一命名，或后端改为返回对象数组。

---

## 问题六：技术栈选型与总设计文档不一致【中】

**位置**：[前端设计.md - 1.1 技术栈](file:///d:/408/前端设计.md#L7) 和 [总设计文档 - 1.3](file:///d:/408/工作台账登记系统-设计文档.md#L15)

**现状**：
| 文档 | 前端交互 | 图标库 |
|------|---------|--------|
| 总设计 | 原生 JavaScript + Fetch API | 无 |
| 前端设计 | Alpine.js 3.x + Fetch API | Bootstrap Icons |

**说明**：前端设计中已充分论证了引入 Alpine.js 的合理性（减少 DOM 操作、声明式绑定、无构建工具依赖），技术选型本身没问题，但总设计文档未同步更新会造成后续开发者的困惑。

**修复**：总设计文档的技术栈表应更新为 Alpine.js，并注明图标库。

---

## 问题七：列表页筛选字段 key 与后端接口参数不一致【中】

**位置**：[前端设计.md - 4.2 状态定义](file:///d:/408/前端设计.md#L287)

前端 `loadList()` 调用 `api.get('/api/ledger', params)`，params 来自 Alpine 状态。但前端定义的字段名需要和[后端 4.2.5](file:///d:/408/后端设计.md#L344) 接口参数名完全对齐。

后端列表接口支持的排序参数 `sort_by` / `sort_order` 在前端设计中**没有对应的 UI 控件和状态字段**——前端缺少排序功能。

**修复**：
- 列表页增加排序支持（默认按 `incident_time` 降序）
- 确保前端 params 的 key 名与后端完全一致

---

## 问题八：详情页图片渲染路径未明确【中】

**位置**：[前端设计.md - 6.1](file:///d:/408/前端设计.md#L553)

详情页展示图片缩略图，但未说明图片 URL 的来源。后端详情接口返回的 `images` 数组包含 `url` 字段（如 `/api/ledger/1/image/a1b2c3d4.jpg`），前端需使用该 URL 作为 `<img src>`。

**修复**：在 6.2 图片查看器部分补充说明图片 src 来源为后端详情接口返回的 `images[].url`。

---

## 问题九：表单提交缺少 PATCH 方法支持【中】

**位置**：[前端设计.md - 3.1 api.js](file:///d:/408/前端设计.md#L119)

前端 `api` 对象只有 `put` 方法，没有 `patch` 方法。编辑提交时（5.6 提交流程）写的是 `api.put(...)`。

**修复**：新增 `patch` 方法，并将 5.6 中的 `api.put` 改为 `api.patch`。

---

## 问题十：图片上传单文件上限与后端不一致【低】

**位置**：[前端设计.md - 5.4 图片上传规范](file:///d:/408/前端设计.md#L498)

| 约束 | 前端 | 后端 |
|------|------|------|
| 单文件上限 | **5 MB** | **10 MB** |

**影响**：5~10MB 的图片被前端过滤，后端配置的 10MB 形同虚设。

**修复**：统一为 10 MB，或保持一致。

---

## 总结

| 优先级 | 数量 | 说明 |
|:---:|:---:|------|
| 🔴 高 | 5 个 | API 方法不一致、缺失接口、图片策略冲突、字段校验不一致、枚举格式不一致 |
| 🟡 中 | 4 个 | 技术栈未同步、排序缺失、图片URL未明确、缺少 PATCH 方法 |
| 🟢 低 | 1 个 | 单文件上限不一致 |
